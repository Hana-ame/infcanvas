# 实现进度（P0 单机 MVP）

> 与 docs/DESIGN.md 对齐的状态记录。每完成一块打勾并注明对应设计章节。

## 北极星
**模拟一个现实的社会**（DESIGN §0）。当前 P0 在验证"小人自主运转"的最小闭环，
神谕/信仰/LLM 属 P1.5+，此处仅保留确定性骨架。

## 已完成（P0）

| 设计项 | 章节 | 状态 | 说明 |
|---|---|---|---|
| 世界生成 | §3 空间模型 | ✅ | Minecraft-like 值噪声（fBm）：海拔+湿度双轴 → 水/沙/草/沙漠/森林/矿/山。出生点 5x5 保证草地 + **近圈资源保证**（饥荒式开局：出生点 3-8 圈确定性撒树/矿/石，开局采矿不落空） |
| 小人自主 AI | §3/§6 | ✅ | 抽 3 选 1 卡系统（种子化不放回）+ 收益择优；基础卡 + DNA 天赋卡（夜猫子/热爱工作/好斗/虔诚/懒惰/强壮/机灵） |
| 意图失真（简化） | §2/§6 | ✅ | 违抗 roll：仅当选中工作卡且有未选本我卡时触发，概率受懒惰/心情/信仰调制，30s 冷却防刷屏 |
| 插槽系统 | §3 插槽/§6 | ✅ | DNA 决定槽位 + 天赋卡占槽；统一卡模型（天赋卡→技能/习惯/神谕策略卡属 P0.5） |
| COC 技能成长 | §3 技能成长 | ✅ | 技能百分制（work/fight/craft/social/faith）；成长=用技能后掷 d100>当前值→+1d10（COC 规则）；技能检定 rollEventSkill（技能越高越稳）；工作→work、战斗→fight；随存档持久化 |
| COC 八属性 | §3 属性卡 | ✅ | **STR/CON/SIZ/DEX/APP/INT/POW/EDU 全八属性**；天赋调制（强壮→STR/SIZ↑、机灵→INT/DEX↑、夜猫子→POW↑）；用途落地：CON+SIZ→HP 上限、POW→SAN 抗压+压制恶意槽、DEX→战斗闪避、STR→采集产出、APP→信仰传播、INT+EDU→技能起点、INT→检定加成；HUD 八属性显示 |
| 七宗罪欲望 | §3 欲望系统 | ✅ | **P0 版**：七宗罪满足度 + 先天罪孽倾向（DNA.sins，天赋调制）；吃→暴食、休息→懒惰、工作→贪婪、祈祷→傲慢；定期检查→欲望驱动卡权重（匮乏的欲望→对应系列卡权重↑）+ 心情影响；恶意槽（长期匮乏→偷窃/砸建筑/暴躁）；HUD 显示、存档持久化。色欲/嫉妒满足途径待 P1 |
| 寻路 | §3 | ✅ | A*（暗黑格 ×3 代价）+ trailCache 缓存 |
| 需求系统 | §3 | ✅ | 饥饿/精力/心情衰减 + 紧急需求（饿→吃、困→睡）+ 饿死 |
| SAN 理智 | §3 SAN | ✅ | 目睹死亡掉理智（距离衰减）；黑夜远离篝火持续流失；篝火旁恢复；<25 狂乱（发呆/乱跑），行为接管 |
| 昼夜 | §3 | ✅ | 120s/天；夜晚精力消耗快 + 屏幕遮罩 + SAN 黑暗恐惧 |
| 环境系统 | §6 环境调制 | ✅ | **天气/气温**（EnvState）：气温随昼夜波动（正午热/深夜凉）+ 天气偏移；确定性雨-晴循环；环境调制卡权重（下雨→户外工作×0.5、娱乐×1.6；酷暑/严寒→户外工作×0.6、生理×1.3）；HUD 显示天气 |
| 马尔可夫偏置 | §6 卡相互作用 | ✅ | **MARKOV_BIAS**：上一轮卡系列→本轮权重偏置（干完活想歇、吃饱想动、闲了想干活…），数据表驱动（mod 可扩展）；lastSeries 决策链记录 |
| 叙事压力 | §6 叙事压力 | ✅ | **和平越久袭击越猛**：和平时长积累 → 战斗压力（1→2×）→ 袭击间隔缩短 + 规模放大（数量、血量）；"和平→压力→冲突"节奏闭环 |
| 生产 | §3 欲望咬合 | ✅ | 伐木/采矿进度+骰子产出、工作台→工具（采集 ×1.3）、农田产粮、矿洞持续产矿（饥荒式矿场，40s 一轮） |
| 篝火光环 | §3/饥荒式 | ✅ | 篝火 = 社会锚点：半径 6 内心情回暖 + 夜晚不易困（SAN 恢复已由 SanSystem 覆盖）；与祈祷/疗伤/SAN 共同构成篝火的多功能定位 |
| 建造 | §3 | ✅ | 篝火/墙/地板/门/农田/工作台/矿洞；蓝图排队 + 幽灵预览 + 建造进度 |
| 战斗/袭击 | §3 | ✅ | 狼群周期性袭击（人数 scaling），攻击小人/建筑，死亡掉落 |
| 伤亡系统 | §3 | ✅ | HP、战斗受伤、饿死、死亡事件（喂给 SAN 与日志） |
| 历史日志 | §3 历史系统 | ✅ | **结构化仿真日志**（HistoryLog）：订阅全部 GameEvent → 条目（时间/天/类型/实体/地点/原因/事实），可查询（type/eid）、可导出（toJSON）、HUD 有 📜 历史面板；事实只来自 sim（LLM 只润色原则就位） |
| 流言/对话 | §6 狗屁倒灶 | ✅ | **微互动层**（确定性模板，零 LLM）：相遇→打招呼/抱怨/表情，心情+好感度双向变化；话题从结构化历史抽取（聊最近的事）→ `social` 事件入历史；HUD feed 显示 💬 闲聊。**传教对抗**（DESIGN §3 对抗检定）：高信仰者尝试说服→传教者(APP/2+faith/2) vs 目标(POW+信仰抵抗)掷骰比成败→成功升信仰+好感、失败反感。闲聊/深聊 LLM 层待 P1 |
| 修理 | §3 | ✅ | 受损建筑自动修理（RepairSystem） |
| 人口 | §3 | ✅ | 死亡/成长（PopulationSystem） |
| 随机事件 | §6/用户Q5 | ✅ | **预制剧本事件系统**（EventSystem + EventProvider 接口）：流浪者加入/丰收/矿脉/瘟疫/游商/庆典，**状况匹配事件列表**（每个事件带 `condition`——有农田才丰收、有余粮才收留流浪者、人多才有瘟疫…只有符合当前局面的事件进候选池，权重+冷却+minTime 调制）；**provider 可插换 = 预留 LLM 插入能力**（P0 确定性随机，P1 换 LLM provider）；事件入历史 |
| 社会关系 | §3/用户Q8 | ✅ | **好感度驱动行为**：亲密(≥40)相邻→心情加成（协作正向反馈）；敌对(≤-20)相邻→口角、积累冲突→动手（STR 判定、掉血、负好感加深，战争萌芽）；关系由社交微互动/传教积累 |
| 派系优先级 | §3/用户Q8 | ✅ | **AI 按环境下达工作优先指令**（RimWorld 工作优先的确定性版）：每 10s 评估资源短缺/建造队列 → 调制全营地对应工作卡权重（食<60→farm↑、木<40→chop↑、矿<15→mine↑、排队→build↑）；随环境自动转变 |
| 自主扩张 | §3/用户Q1/Q8 | ✅ | **AutonomousBuildSystem**：AI 评估资源与营地状态自动规划扩建（无篝火→起篝火、人多吃紧→加火、缺粮→扩农田、缺工具→工作台、矿少→矿洞、富余→围墙、**信仰高→建教堂**）；20-30s 评估一次注入 buildQueue，小人照常执行；营地自主生长（观察模拟器核心） |
| 教堂 + 神谕 | §3/用户Q2/Q3 | ✅ | **教堂**（2x2，信仰≥35 自动建）+ **神谕影响**：玩家选中教堂→"发布神谕"按钮→祝福附近(半径6)高信仰小人（信任=信仰/100 过滤，低信仰不受影响）：30s 心情 buff + 信仰↑；教堂是神谕唯一物理接口（DESIGN §3）；神谕不直接指挥，只影响目标层 |
| 社会单位/部落记忆 | 用户Q9+即时指令 | ✅ | **有篝火 = 独立派系**。每个篝火 = SocialUnit：部落记忆（成员/事件/看法）、名字、成员归属。**教堂 = 篝火升级**（容量：篝火记 2-3 连接、教堂 5-10）。**篝火间信任机制**：成员协作→看法增；双向友好→贸易（木换食）；双向敌对→派掠夺者攻打对方（战争）→加深仇恨；**派系 = 单位间的看法关系**（友好→贸易/伙伴、敌对→战争，无抽象联盟层）：双向敌对→派掠夺者攻打对方（战争）→加深仇恨。出生点初始篝火=首派系；野生篝火事件刷新新势力 |
| 存档 | §5 | ✅ | **IndexedDB**（异步、大容量），30s 自动存档；旧 localStorage 存档已弃用；含社会单位（派系/记忆/看法/库存/玩家所属）持久化 |
| 渲染 | §1 | ✅ | PixiJS v8；SVG 素材（btoa data-uri → GraphicsContext）；地形/建筑/小人/敌人生成 |
| 视角切换 | §1 渲染 | ✅ | **2D 俯视 ↔ 2.5D 同轴**（右下角按钮）；2.5D 按世界 y 排序实现前后遮挡（树/建筑/小人/敌人统一进 entityLayer，zIndex=y×10+层级） |
| HUD | §1 | ✅ | 资源条、建造菜单、速度控制（⏸/1x/2x/3x）、缩放按钮（+/-）+ PageUp/PageDown、帮助、事件 feed、选中面板（COC 属性/天赋/插槽/需求/HP/信仰/理智/闪念）、建筑面板 |
| 测试 | §6 | ✅ | vitest：RNG 确定性、地形确定性、抽卡、移动/建造/采矿闭环、SAN 目睹死亡/篝火恢复 |
| mod 注册表 | §7 | ✅ | **ModRegistry**（DESIGN §7 扩展性原则）：运行时注册 tile/building/item/**enemy**/card/recipe/event/expansionPlan/intent/work/system/hook + overrideDef/overrideTuning + 冲突检测；`SimOptions.mods` 构造期挂载；详见 docs/DATA_DRIVEN.md（§6 验收 17 项全 ✅，89 测试覆盖） |
| 数据驱动化 | §7 | ✅ | **sim 层 defId 特判清零**：craft 自配方/发光/升级(upgradesTo)/神谕(capabilities)/派系优先级表/敌对种类(enemies.ts)/派系袭击(raider def)/采集目标(growable+harvest)/采后瓦片(harvestReplaces)/光环(aura.radius)/AI 建造成本(def.cost)/出生建筑(tuning) 全部走 defs/tuning/registry，mod 不改内核（DATA_DRIVEN.md §6） |
| 存档健壮性 | §5 | ✅ | save/load JSON-safe：slots 存卡 id 还原（不再存闭包）、load 重建成员归属防假团灭、修复重建时 splice 跳杀残留 pawn |
| P1 server 骨架 | §5/§8/§9 | ✅ | **权威在 server**：Node 复用 `src/sim`（零 DOM ✓ tsx 直跑）+ WSS 通道；协议 `src/shared/protocol.ts`（welcome 全量 tile+defs 只读表 / 2Hz snapshot / tileChanged+feed 增量 / C→S 复用 Sim.Command）；server：固定步进 accumulator + `addTileListener` 增量推送（world.setTile 相同值去重不触发）；client：`?remote=ws://…` 连入，`RemoteSim` 实现与本地 Sim 同构读取面（world/pawns/建筑/需求/属性/欲望/卡池/事件），HUD+Renderer 双端共用；命令经 WSS 上行、server 权威执行、快照回显（e2e：scripts/e2e/remote-viewer.mjs 8 断言全过）。**断线重连（补强）**：client 指数退避自动重连（1s→2s→4s…封顶 15s）+ 首连失败明确报错（红屏提示配置错误）+ 顶部 reconnect hint（`remote-hint`）；**看门狗兜底**：connected 后 5s 无任何消息即判定 server 假死/网络黑洞 → 主动断开重连（不依赖 TCP 超时，`RemoteSim.watchdogMs` 可调）；server kill/重启 e2e 冒烟页面自动恢复（reconnect.test.ts 6 用例） |
| tick delta 增量 | §8/§9 | ✅ | **快照增量（P2 第一块）**：`src/server/diff.ts` 纯函数对比相邻快照 → 只发变化（`DeltaMsg`）：pawn 按 eid 逐字段 diff（x/y/hp/job/needs/faith/skills/slots/desires…）、新 pawn 首现必带 attrs、死亡 removed+pawnList；建筑按 key(y*width+x) 对齐 diff hp、拆除 removed；hostiles/stockpile/buildQueue 整体覆盖；全局字段各自携带。server：500ms 一轮 diff 广播 + 5s 全量对账（防增量丢失/相消漂移）+ 新连接先收全量底（广播过的快照即 diff 基线）；client：`applyDelta` 合并进 pawnCache/世界（读取面零改动，HUD/Renderer 无需区分）。12s e2e 实测 delta 26 帧 vs snapshot 4 帧（带宽 -90%）。测试：diff.test.ts 7 用例 + reconnect.test.ts applyDelta 1 用例 + scripts/e2e/remote-delta.mjs（帧构成/时间/位置断言） |
| 渲染插值 | §1/§8 | ✅ | **位置插值（渲染平滑）**：delta 500ms 一跳 → 渲染层 `interpPos`（pawn + 敌对）按 sim 时间线性插值（段起终点 + t0/t1，k 线性收敛）；`RemoteSim.renderNow()`：**播放时钟**——权威 t 锚定墙钟 + 帧间 extrapolate（speed 加权，paused 冻结），否则 time 只在消息到达时跳变、插值恒 k=1 失效；本地模式同样走插值（每 tick 段极短 ≈ 贴合真实）。顺带修复：死亡/重生 pawn 的渲染残留清理（delta 下 removed 广播后 sprite 及时移除，本地模式同受益）。e2e：scripts/e2e/remote-interp.mjs 采样 60 帧 sprite 位置出现 6-13 个渐进中间值（非 48px 跳变） |
| LLM 慢决策层 | §6/§10 | ✅ | **`LLM_ENDPOINT` 环境变量即启用**（OpenAI 兼容 chat completions）：`src/server/llm.ts` 预取模型（后台拉取 → 事件队列 → EventSystem 同步消费，失败指数退避降级确定性）；LLM 输出 JSON schema（name/text/effects）→ 白名单执行器（resource/mood/hp/recruit，数值钳制，**不进选择链路** ✓ DESIGN §6）；世界摘要随请求携带；`SimOptions.eventProvider` 构造注入。冒烟：mock LLM → server → feed 广播 `🎁 获得 ore 10 ✨ 星陨之夜` |
| 命令权威校验 | §8/§9 | ✅ | **联机安全（P2 前置）**：server `src/server/cmdValidate.ts` 把关上行命令——类型白名单/形状/坐标越界/建筑与职业存在性/`pawnId` 存在性/令牌桶频率（30 条/s，per-client 连接级）；非法命令丢弃并记录（不踢连接）；client 的 build/oracle/assign/move 显式带 pawnId（观察模式 server 无 selected 镜像）+ sim.issueCommand 的 assign 分支支持 pawnId。测试：cmdValidate.test.ts 5 用例 + viewer e2e 9 断言（build 回显 / move 指挥位移） |

## 分支实验记录（feature/colony-survival-dims，2026-08-13）

### 实验目标
四维度殖民地（取水/娱乐/食物/建材）+ 房屋/玩具建筑 + 科技初始只给娱乐（探索式开局），验证能否跑起来。

### 发现并修复的问题（分析报告）

**1. 探索卡蓝图无限入队**（测试超时）
- 现象：探索卡反复触发 → 蓝图堆积 → 队列爆炸
- 根因：探索执行器无去重（神谕垦田令有 buildQueue.some 去重，探索漏了）
- 修复：蓝图已在队列（同 defId）→ 跳过
- 注释：cardSystem.ts execExplore「蓝图已在队列（重复探索）→ 跳过（防蓝图堆积：垦田令同款去重）」

**2. 寻路风暴（性能爆炸，217 倍修复）**
- 现象：模拟 175s 后单步 50ms+，6000 步 99s（cpu profile：findPathRaw 58.9% + findPath 15.7% + GC 11.4%）
- 根因：狼袭击打断小人路径 → path 反复重建 → 每帧寻路 miss（trailCache 命中率低）+ A* 堆分配 → GC 风暴
- 修复（用户指令"寻路次数不要每帧都发生，缓存每一步走 + 限定最大距离"）：
  - `tuning.path.pathCd`（0.5s）寻路节流：moveAdjacent 在冷却内不寻路
  - `tuning.pawn.maxWorkDist`（36）工作目标最大距离：超距不寻路（玩家命令 moveTo 不限）
  - farScanRadius 45→36 与 maxWorkDist 一致（远扫目标超距会被拒，避免白扫）
- 结果：6000 步 99s → 457ms
- 注释：sim.ts moveAdjacent 完整根因记录

**3. 探索卡执行器解析失败**
- 现象：抽到探索卡但建筑永远不建（toy=0）
- 根因：执行器从 label 解析建筑 id，但 label 是科技名（探索·玩具工艺 ≠ 'toy'）
- 修复：label 规范为 `探索:${buildingId}`（机器可解析），显示名走 name 字段

**4. 水井产出丢失（water 恒 0）**
- 现象：well 建成但 water 库存 0
- 根因：FarmSystem 只处理 tags 含 'farm' 的建筑，水井（passive recipe）被跳过
- 修复：FarmSystem 泛化——处理所有 passive recipe 建筑（农田/水井/mod 建筑通用）
- 注释：farmSystem.ts 文件头「曾踩坑：只认 farm tag 导致水井 water 永不产出」

**5. 探索卡抽不到（择优竞争）**
- 现象：探索卡在池中但从不执行
- 根因：utility 16 低于其他卡，pickBest 择优永远不选
- 修复：utility 40 + weight 8（娱乐语境下的探索冲动：抽到即可执行）

### 重玩验证（修复后）
- **渐进权重确认**：科技解锁初期只有娱乐探索命中（D2 解锁→立即"灵光一现建水井"），
  权重 ramp 300s 后普通建造接管（techBuildWeight 0→1，workBuild 无队列按权重规划）
- **水维度归集修复**（💧 恒 0 → 500）：玩家建的建筑（well）产水被野营 campfire 抢归集
  （addProductionNear 按"最近单位"归集）→ 改为按建筑 faction：'player' 建筑产出进全局生产池；
  转发层 Sim.addProductionNear 补 faction 参数（曾漏传导致修复无效）
- **平衡**：buildingDmg 6→3（well 被 5 狼 3 秒拆 → 反复重建循环）；探索落点 3-6→2-5（靠内圈）
- 长局 3600s：💧500(cap) 🍖402 🌲562 👥9-13 稳定，寻路风暴无复发（30s 跑完）

### 实验结果：殖民地能跑起来 ✅
- 人口 4→10（流浪者持续加入）；toy/well/house/raft 全部通过"娱乐探索"建成
- 科技顺序解锁：toyTech→wellTech→houseTech→raftTech→bridgeTech→boatTech
- 终局：👥10 💧386 🍖355 🌲500，四维度全通，无团灭

## 待办 / 差距

> 旧条目若已在上表 ✅ 则移除；以下为当前真实差距（按优先级排序，2026-08-10 核）。

| 项 | 目标（DESIGN） | 差距 |
|---|---|---|
| client 层数据驱动 UI | §7 分层原则（服务端 mod 逻辑 + 客户端 mod 表现） | ✅ **已打通**：建造菜单遍历 `sim.mods.buildings`、tile 渲染查 `sim.mods.tiles`（未知 id 兜底色不崩）、tile/建筑图标可 `sprite` 声明复用素材、敌人按 enemyId 散列着色；`?mods=url` 运行时加载 ESM mod（demo: src/mods/demo-berry.ts，e2e: scripts/e2e）。剩余：mod 自带上传 SVG 素材管线未做（暂复用内置 sprite） |
| 闭合类型开放 | §7 mod 扩展 | `BehaviorCard.series`/`DesireId` 已开放为 string + `BehaviorCard.desire`(欲望关联)、`satisfies` 已有；新增 `ModRegistry.registerDesire(id,label)`（新欲望维度自动进循环：初始/衰减/匮乏/恶意/满足）；**`BehaviorIntent.action`/`UnitLevel` 已开放为 string**（`registerIntent(id, executor)` 全链路：mod 卡 decide 产出任意 action → 行为系统 Map 分派；`registerUnitLevel(id, capacity)` 新派系等级 + 记忆/看法容量，未知等级回退最小容量）；HUD 欲望显示遍历 DESIRES 表。mod 通路测试：自定义 intent 卡全链路执行 + temple 等级容量 20 |
| 插槽保底 | §6 插槽 | ✅ **已修**：initSlots 保底 3 张基础卡（eat/rest/chop），maxSlots=2+2trait 不再"永久闲逛"（曾实测）；HUD 文案改为「卡池 n 张（槽 m）」 |
| 丢失 chew：出生点/野营 'campfire'、派系掠夺者已数据化 | §7 | 出生建筑已读 `autobuild.starterBuilding`；`scripts.spawnWildCamp` 仍写死 'campfire'（语义上"野生营地=篝火"成立，暂留） |
| 流言/对话完整 | §6 | 微互动已做（模板）；**话题沿社交网络传播已落地**（2026-08-12）：小人记住听到的八卦（`gossip` 字段），TTL 内（`social.gossipTtl` 60s，`gossipChance` 0.7）转述给下一个相遇者，`gossip_spread` 事件可观测，确定性模板（零 LLM）；剩余：闲聊/深聊（引用记忆的 LLM 对话）待 P1 |
| 七宗罪欲望完整 | §3 欲望系统 | ✅ **七途径全通**（2026-08-12）：暴食(进食)/懒惰(休息)/贪婪(工作 satisfies)/暴怒(战斗+恶意槽)/傲慢(祈祷) + **色欲**（正向社交互动，`social.lustFulfillPerInteract`）+ **嫉妒**（存在更强同伴时完成劳动，`desire.envyFulfillPerWork`，skillTotal 标杆） |
| COC 属性全用途 | §3 属性卡 | ✅ **SIZ 负重已落地**（2026-08-12）：采集一次搬回量 = `carryBase + max(0, SIZ-strBase)×carryPerSiz`（tuning.gather，默认 4/0.5），矿洞/采矿/伐木产出统一钳制（保底 1），与 STR 产出加成互补；单测 + 集成测试（`carryCapOf`/`capGainTo`） |
| LLM 层 | §6 | ✅ **已落地**：server `LLM_ENDPOINT` 即启用（OpenAI 兼容），预取+白名单效果+降级（见上表）；剩余：真实 LLM 冒烟（本机无出网 key）、provider 频率分级（付费/免费，DESIGN §10）、LLM 叙述进"历史/记忆"而非仅 feed |
| server 增量优化 | §8 | P2 继续 | ✅ **tick delta 已落地**（500ms 增量 + 5s 对账，见上表）；剩余：实体事件化推送、chunk 按需、插值、兴趣管理、客户端权威提交验证待 P2 联机 |
| mod 打包/沙箱 | §10 待定 | **自包含 .mod.json 包**（manifest+defs 纯 JSON 声明+scripts 函数式）✅：loader 校验/白名单沙箱/幂等挂载（166 测试）；**服务端 mod 管理器** ✅：`mods/*.mod.json` 自动挂载（坏包清晰报错拒服）、`MODS_DIR` 可配、`SimOptions.registry` 预建表；**客户端** ✅：`?mods=*.mod.json` 包或 ESM 源码双通道；CLI：`npm run mod:pack`。剩余：zip 批量/素材打包、联机 mod 同步校验 |
| 联机 | §8 | P2：WSS 协议、多客户端同步、兴趣管理、持久化、鉴权 | ✅ 命令权威校验已提前（见上表）：形状/范围/pawnId/频率把关 + e2e 断言；剩余：多客户端、兴趣管理、持久化、鉴权 |

## 技术栈

- Vite + TypeScript + PixiJS v8 + bitecs ECS（SoA）+ vitest
- sim = 纯 TS 双端共用（`src/sim/` 零 DOM/PIXI），P1 起 server 直接 import
- 确定性：SimRng（种子化）+ 固定系统顺序 + 独立 world chunk 生成

## 存档注意

- IndexedDB 库名 `infcanvas-db`，store `saves`，key `autosave`
- 旧 localStorage `infcanvas-save*` 已不再读写（版本切换，旧存档作废）
| 兴趣驱动娱乐 | §6 娱乐 | ✅ **已落地**（2026-08-13）：娱乐从固定小卡池（闲逛/探索）改为**开放活动空间**——做什么由 pawn **兴趣属性**决定。起因：试玩发现 toy 被反复建 39 次吃光木头（toy:39/well:2/house:1）；先试「buildMinWood 游牧期门槛」拦截全部科技建造被用户否决（治标），改为兴趣属性治本。每人随机 1-3 个兴趣（`defs/interests.ts` 表驱动，gather/mine/fish/build/pray/wander/rest），兴趣带专属休闲卡进卡槽（initSlots）；带 `interest` 标记的卡（探索卡标 `build`）由 `ruleInterest` 按有无该兴趣调制权重（有兴趣 ×weightMul / 无兴趣 ÷weightMul）。效果：toy 建造 39→4 次，house 2、well 5，终局木 392（不再枯竭）。`techBuildChance` 同门控（无 build 兴趣不主动建科技建筑）。mod 可 `registerInterest`（或 `.mod.json` defs 声明 `interests`）扩展 |
| 玩家不参与模拟循环 | §5 权威模型 | ✅ **架构裁决落地**（2026-08-13）：**删除"玩家单位"实体**——模拟循环里只有自主派系，玩家不存在于循环中，只有通过**卡片**（神谕降旨/策略卡/printCard）与**指令**（issueCommand）干预。删除 `playerUnitId` 字段/`checkPossession` 团灭附身/`allocateResources` 玩家镜像/贸易 isPlayer 特判；全局仓库 `stockpile` 即玩家资源池（`faction='player'` 建筑产出直接进全局），各派系按成员数自给独立库存。相关测试改写（生产归集语义、筏上渔获确定性驱动），225 全过 |
| 篝火 = 区域历史载体（B 方案） | §930 派系涌现 | ✅ **落地**（2026-08-14）：用户设计「每个人保存一个篝火，在篝火周围生存；不舒适可另起篝火；篝火记载区域生活/历史；同 chunk 相近才可交流篝火情况；pawn 据此判断伙伴/敌人；记得个体间社会关系」。实现：`pawn.fireId`（归属篝火）+ `pawn.knownFires`（对听说的篝火的 stance/basis/at）；篝火 history = 区域事件（建筑/袭击/摧毁/战死，bus 订阅写入）；`exchangeFireStory` 交流（同 chunk 相遇、冷却防刷屏）→ 从历史关键词推断 enemy/friend；`migrateIfUncomfortable` 另起篝火（遭袭计数 raidCount ≥ 阈值才迁，防连锁分裂——首版仅凭"敌人近"致 15 空壳派系，改按篝火遭袭计数）。测试 +4，229 全过 |
| 派系碎片化治理 + 归属持续收敛 | B 方案连锁雪崩修复 | ✅ **落地**（2026-08-14，试玩暴露）：① **migrate 判据三修**——首版"敌人近"迁 → 狼驱散文明（12 次/15 空壳）；二版"狼在场计数" → 狼扫过一遍 raidCount 疯涨（90 分钟 40 次另起、41 单位 34 空壳）；终版**仅真实建筑被毁（💥 入史）才计数**，狼路过不算（可战斗/逃跑），新篝火落点远离旧营地 ≥migrateMinDist 防连锁再迁 → 收敛至 4 单位。② **归属持续收敛**——归属原只在"建 campfire/出生/迁徙"瞬间算，小人走到营地旁不重算 → 大量"人在营地旁却无火"游牧幽灵；加低频全量 reassign（20s）自然划入。③ **营地被毁清 fireId**——只删 membership 没清 fireId → 幽灵归属；修复。④ 测试 +2（营地被毁清火 / migrate 真实损失判据），230 全过 |
| 删派系实体层：派系 = 纯涌现展示 | 用户 2026-08-14 裁决「不要派系系统，派系单纯用涌现展示」 | ✅ **落地**：删除 SocialUnit 类型/units/membership/单位私有库存/单位间贸易战争传话/征服/单位命名升级/faction_event。保留并迁移涌现层：`pawn.fireId` 改指向 **campfire 建筑 key**；区域历史 memory 挂到 **campfire 建筑**（world.fireMemory）；`factionsView()` 运行时按 fireId 聚合（纯只读展示，无库存/贸易/战争）；另起篝火 migrate 纯建筑级。HUD 派系面板改为篝火聚居展示。测试删除派系实体用例、B 方案测试改造为涌现版。218 全过 |
| 私有食物 + 互助卡 + 需求入篝火史 | 用户 2026-08-14 设计「好感高→帮忙=满足食物娱乐；私有物品；篝火记载需求」 | ✅ **落地**（仅食物私有化，克制范围）：① `pawn.inventory` 私有食物——主动采集（矿洞/渔获）食入口袋，进食优先扣个人、无则公共粮仓兜底；木材/矿石仍全局。② **互助卡**（base 卡常驻槽位，weight 12）：附近有"缺食/受伤/低落"且我好感 ≥ helpFriendAt 的邻人，且自身稳定时可选；execHelp 送食（口袋转移）/疗伤/陪伴，受助方好感提升（互惠）。③ **需求写篝火历史**：濒死/低落小人的需求记入附近 campfire 记忆，经交流传播。测试 +4（私有食物入袋/进食优先个人/互助闭环/需求入史），222 全过。落井下石（敌视者不援手/破坏）按用户指示暂不做 |
| 世界观修正：野狼 → 野猫 | 用户指出「背景设定是鼠鼠和猫」，狼是早期幻觉设定 | ✅ 落地（2026-08-14）：enemies 表 wolf→cat（野猫），raidEnemy/catSpeed/catLoot、raidSystem 日志、篝火记忆文案、渲染 tint、HUD 文案、注释全部替换；测试同步。docs 旧行按铁律保留并追加更正段 |
| 插件可卸载 + 采集狩猎玩法包 | 用户 2026-08-14 指摘「为什么不能卸载插件」 | ✅ **落地**：① `ModRegistry.disableSystem(id)`——装配过滤，mod 可卸载内置系统（farm/craft/techPool/autobuild/repair 等），"一切皆插件"从"只能加"补全为"可撤"。② **hunter-gatherer mod**（src/mods/hunter-gatherer.ts）：卸载 5 个生产系统 → 纯采集+狩猎；猫掉肉 food（击杀私有化进个人口袋，raidSystem 掉落逻辑适配）；huntWildSpawn 野外常驻猎物 + huntCombat 狩猎攻击推进 + 狩猎卡；营火自动重建（无 autobuild 时营地被拆会散落）。模拟 90 分钟：猎杀 37 次、人口 9/10 稳定、食物 439、口袋私有化生效。测试 +2，224 全过 |
| 单系统独立测试（最小 ctx，16 系统全覆盖） | 用户 2026-08-14：为了支持一个个系统分别测试，请确保插件化 | ✅ **落地**：① `src/sim/__tests__/helpers/minCtx.ts`——最小 SimContext 测试助手（真实 World/EventBus/Rng/ModRegistry/tuning + 桩方法），系统可脱离完整 Sim 单独装配测试。② 16 个系统各建独立测试文件（needs/san/population/gather/build/craft/farm/repair/raid/desire/social/socialUnit/techPool/autobuild/event/card），52 用例全过。③ **打破 registry↔pawn 循环依赖**：cardPredicateOf/weightRulesOf/socialLinesOf 与 static store 移到 `src/sim/mods/query.ts`（原定义在 registry，pawn 运行时依赖它 → 模块级循环 import，加载顺序一变就报 "cardPredicateOf is not a function"）。测试 276 全过 |
| 测试加固（审阅后） | 用户审阅测试方案后要求加固 | ✅ **落地**（2026-08-14，用例数不变仍 276）：① **gather 私有化假通过修复**——默认 TILES 无产 food 的 tile（树→wood/矿→ore），旧条件式断言从未走到私有化分支；改为自定义 World 注入浆果丛 tile（growable+harvest food）真实触发私有化路径（食物进个人口袋、全局 0）。② **静默 return → 显式断言**：craft/build 的 `if (!def) return` 改为 `expect(def).toBeDefined()`（内置表成员缺失即测试失败，不再假通过）。③ **私有字段解耦**：eventSystem 测试弃 `sys2['lastTrigger']`（与实现耦合），改用 `event_happened` bus 计数断言触发次数。④ **card 用例清理**：删残留旧注释，补 `picked ∈ drawn` 硬断言。⑤ **socialUnit 迁移用例**：删多余的第二个系统实例（原"双实例共享 bus 订阅"误导写法），同实例直接 update |
| 卸载不破坏核心（插件化加固） | 用户 2026-08-14 要求"查看是否真正插件化" | ✅ **落地**：审阅装配面发现两处违例——① 构造器在 registerSystems 之外预建 BehaviorSystem/SocialUnitSystem（禁用 behavior 时孤儿实例吞掉 intents 挂接）；② socialUnit 卸载后 sim 的 bus 回调/归属/记忆 6 处调用空引用崩溃。修复：构造器删预建（registerSystems 唯一实例化点）；`sim.socialUnits` 默认回落 `NOOP_SOCIAL_UNITS` 空实现（消费方契约不变）；intents/works 挂接判空条件化。回归保护：`src/sim/__tests__/uninstall.test.ts` 20 用例——16 系统逐个卸载 + socialUnit no-op 契约 + behavior 卸载 + 采集狩猎组合卸载（600s 长跑）+ 全量卸载空壳，全过。全量 296 测试。差距待裁决：farm/craft/techPool/autobuild/repair 仍内置内核，玩法包靠 disableSystem 卸载达成"可撤"、未迁移为"默认玩法 mod" |
| 玩法包化（正向组装） | 用户 2026-08-14 裁决"最终模拟器 = mod/插件一个个添加玩法组装" | ✅ **落地**：内核 `SYSTEM_DEFS` 收窄为 11 基础系统（needs/san/desire/behavior/socialUnit/social/gather/build/raid/population/events）；玩法系统迁出为 5 个独立玩法包 `src/mods/packs/`（farming/crafting/repair/tech-pool/autobuild），`ModRegistry.default()` 默认挂载全部 = 原 16 系统完整模拟器（体验不变）。同锚点保序修复（多 mod 同 before 按注册序排，产出序 farm→craft→repair→raid）。验证：`assembly.test.ts` 5 用例 + 卸载测试遍历完整装配集。全量 301 测试 |
| UIUX 增强（状态图标/选中光环/建造进度/移动箭头） | 用户 2026-08-14 要求"进一步"UIUX | ✅ **落地**：① 小人头顶状态图标——伤(❤️🩹 hp<40%) > 狂乱(😵 san<25) > 饿(🍗 food<30) > 困(😴 rest<20) 一次只显最紧急（阈值对齐 tuning hungerAt=30/crazyAt=25；rest 无 urgentAt 暴露给渲染层取 20），Text 池随小人生命周期增删不每帧新建。② 选中高亮圆环——黄色圆环跟随选中 pawn（含插值），替代原仅 scale 1.15 的弱反馈。③ 建造进度条——协议 buildQueue 带 progress（快照+delta，server 端取整省带宽），蓝图格底部按 progress/buildTime 实时填充，mod 建筑同样生效。④ 移动箭头——右键移动从起点 pawn 画方向线+箭簇+目标圆环（2.5D 锚格底适配）。⑤ 修复帮助面板过时文案"派系间会贸易/传话/袭击/吞并"→营地涌现口径。全量 301 测试 + tsc 干净 |
| UIUX 增强 2（反缩放/血条/职业标签/历史跳转） | 用户 2026-08-14 继续"进一步" | ✅ **落地**：① **反缩放 UI**——状态图标/选中圆环线宽/移动箭头线宽/血条按 1/zoom 反算，屏幕恒定大小（zoom 0.3 不再缩成芝麻、zoom 4 不再占满格）。② **血条层**——hostile 全部显示头顶血条（绿>60% 橙>35% 红，高 4 屏幕 px），选中 pawn 头顶也显示，替代原 alpha 变暗。③ **选中职业标签**——选中 pawn 头顶显示职业名（jobLabelOf，随 1/zoom 缩放）。④ **历史面板点击跳转**——createHud 加 onJumpTo 回调，历史行带 data-x/y，点击带 📍 的行镜头直接跳转并收起面板（委托监听一次，innerHTML 每帧重建不丢）。全量 301 测试 + tsc 干净 |
| 玩法包框架审查修复（依赖图/远程加载） | 2026-08-14 框架审查发现 pack 框架 3 处硬伤 | ✅ **落地**：① **循环依赖检测死代码 → 无限递归**——`resolvePack` 的 `mountingStack.push` 原在 deps 循环之后，递归解析时栈恒空、检测永不触发（实测 A↔B 环 → `Maximum call stack size exceeded`）；改为先入栈再处理 deps（finally pop），环 → 明确 `/循环依赖/` 报错且不半挂（双方 apply 均不执行）。② **loadRemote 跨环境断裂**——原 `Buffer.from`（Node-only，浏览器 ReferenceError）+ `new Function` 动态 import（vitest/CJS 下 "dynamic import callback was not specified"，远程 3 用例全挂）；改为模块作用域 `await import(/* @vite-ignore */ data URL)` + `encodeURIComponent`，浏览器/vitest/tsx 三端通。③ **registerPack 同 id 异对象抛错**——全局包目录跨测试互相污染（同 id 不同对象冲突），且 vite HMR 重新 import = 新对象 → dev 挂载必挂；语义改为 warn + 替换（last wins，def 级注册冲突仍由 assertNew 兜底）。④ 附带修复：trade.ts apply 缩进、wildmouse requires:[] 冗余、远程用例隐藏缺陷（bare registry 无 campfire 却 overrideDef → 换注册新物品断言）。回归保护：`pack-deps.test.ts` 9 用例全过（含真环路径/不半挂/栈干净/替换语义）。全量 318 测试 + tsc 干净 |
| 科技碎片制 | 用户 2026-08-14：「不要有什么做什么，科技是这样的，每个科技都有碎片，碎片攒齐了才能组成科技，也是抽卡」 | ✅ **落地**：科技从"抽到整卡即解锁"改为**碎片制**——`TechDef.fragments`（缺省 1，内置 7 科技统一 3 块）；`sim.techFragments` + `grantTechFragment`（攒满自动解锁、已解锁幂等防刷）+ `fragmentsNeeded`；存档随档（旧档兼容从零攒）。抽卡端（tech-pool 玩法包）改为**发碎片**：候选 = 未解锁科技，权重按 TECH_ORDER 线性递减（rank0 最高）——保留"往后抽卡"渐进（靠前先攒齐）又保留抽卡随机性。UI：HUD 科技面板显 🔩 x/3 进度、play.ts techs 命令碎片视图、历史日志「🔩 拾获/🔬 科技完成」。顺手修复：play.ts 传了从未生效的 `techInterval` 选项（DummyPlannerOpts 无此字段，tsc 不管 scripts/ 一直静默失效）→ 删除并注明；techPoolSystem 旧测试桩 rng 缺 weightedPick → 适配碎片制重写。验证：techs.test.ts +4（攒满才解锁/存档往返/旧档兼容/抽卡池 21 碎片全解锁）、techPoolSystem 3 用例改造，模拟 20 分钟 → 玩具工艺 ✅ + 取水术/木屋营造各 1/3。全量 322 测试 + tsc 干净 |
| 采集狩猎玩法包调优（试玩剧本 1-4 + 长跑） | 用户 2026-08-14「多玩几把，玩别的方法」剧本试玩暴露 | ✅ **落地**：① **play.ts f 模式两处 CLI bug**——管道 EOF 后 rl.question 抛 ERR_USE_AFTER_CLOSE 崩溃（ask 包 try/catch + 命中 exit）；f 的 `once('line')` 吞掉暂停行（state 不执行）→ 改为 `running=false + handle(line)`。② **sanSystem 永久崩溃死锁（内核）**——崩溃者逃到篝火旁后 crazyTime 重置却继续落下去乱跑走开 → 永远离开火堆、SAN 恒 0（hg 局 8/11 锁死，人在火旁 4-13 格不恢复）；修复：火旁任何时刻 return 呆着等恢复。③ **hg 猎杀闭环**——huntNearby 索敌半径 25→40（猫环带 15-40 营地外，25 时几乎触发不了）；追猫超时 25s→45s（环带平均 27 格步行 27-42s > 25s 目标永远被清）；hunt 卡 weight 7 + 白天谓词 huntIsDay（夜追猫全员夜宿崩溃）。④ **夜归篝火卡**（hg）：内核无"天黑回营"行为 → 夜晚+离火>3 格 → 高权重（80）回营卡 work:camp（moveAdjacent 最近 campfire）；配套 CardView.campfireDist 钩子（mod 谓词注册口）。⑤ **卸载 medicine 玩法包**（hg，真凶）：猫咬掉血 → bleed 流血伤口 → san -0.6/s/条 → 崩溃者被 job 覆盖无法治疗 → 伤口持续 → SAN 恒 0 永久锁死（30 分钟局 12/13 崩溃的真凶，非 nightDrain/trauma）；hg 游牧伤势不扰心智，伤口层留给定居包。⑥ **hg tuning**：nightDrain 0.02（1.2 点/夜）、crazyFleeAfter 60→15（崩溃者当天窗口内回火恢复）、needs.sanTraumaDrain 0.01、fireComfortRadius 9。验证：30 分钟 hg 局 0 崩溃、人口 4→14、猎杀 82 次（肉 289 个人口袋）、篝火 4 聚居 4 死亡 0、wall ~30s。回归测试 +4（san 火旁呆着恢复 / hg 装配含 medicine 卸载 / 猎杀掉肉私有化 / huntIsDay 谓词）。全量 326 测试 + tsc 干净 |
| 再玩四局（2026-08-14 第二轮） | 用户「再玩玩看，玩别的方法」 | ✅ **落地**：① **medicine 伤口封顶**（真凶 #2）——默认局 30 分钟 3/10 人 SAN 恒 0 永久崩溃：猫狼反复咬 → 26 条伤口无上限堆叠，bleed×10+ = -6/s 流失 > 篝火恢复 2.5/s → 火旁也恢复不起来；治疗 4s/条 30 分钟治不完。修复（medicine 包自治，不动内核）：bruise/bleed 各封顶 3 条、infection 1 条（最大流失 -1.8/s < 恢复 ✓），感染转化同步封顶。默认局复测：0 崩溃、人口 13、伤口 ≤6、治疗事件正常。② **拒建反馈**——build 指令撞占用/未知建筑/资源不足全部静默拒绝，玩家毫无反馈（贸易站指令撞 fence 反复点没反应）；queueBuild 各拒因分支 logEvent 明确原因（📛 建造被拒：…）。③ **剧本复玩**：游牧建新据点 ✓（#1 远行 (130,60) 5 分钟建成第二篝火，孤身开拓；migrate 仍仅"不舒服"触发、无同伴跟随属设计内）；商贸 ✓（贸易站 1 座、20 分钟商队到访 6 次）；出走 ✓（单人 10 分钟野外战死，campaign 验证）；hg 狩猎 ✓（5 分钟猎杀 5 次、30 分钟 82 次）。回归测试 +4（medicine 封顶×3、拒建反馈）。全量 330 测试 + tsc 干净 |
| medicine 包 RimWorld 化重构 | 用户 2026-08-14「参考 rimworld 的受伤规则」 | ✅ **落地**（完整版含部位）：伤口从枚举字符串升级为**实体** {类型 cut/bruise/burn、部位 head/torso/limb、愈合进度 0-1、出血中、感染度 0-1}（旧档字符串自动迁移）。RimWorld 规则：ⓐ **自然愈合**——不治疗也会随时间好转（cut 60s/bruise 90s/burn 120s 满 1 痊愈自动移除，愈合让伤口数量自我收敛）；ⓑ **出血**——cut 新伤失血(hp) + 心慌(san)，愈合过半自然凝血（小伤自愈，大伤需治疗止血，不治会失血而亡）；ⓒ **感染=概率事件 + 赛跑**——未处理伤口 60s 检定 2% 感染（头伤两倍，RimWorld：感染非必然），感染后感染度按部位增长（头快四肢慢）+ 持续掉血/剧痛/理智流失，到 1 坏疽态，治疗成功感染度大幅回落（免疫胜利）；ⓓ **疼痛**——所有伤口产生心情惩罚随愈合消退（新伤最痛）；ⓔ **部位**——命中权重 limb 60%/torso 30%/head 10%，头伤失血×2 疼痛×1.5（重伤位）、躯干×1.5、四肢×1；ⓕ **治疗 = 处理**（triage：感染 > 出血 > 新伤，成功止血+退感染+加速愈合，非移除）。修复中暴露 2 个设计缺陷：演化掉血（出血/感染）会触发自身受伤检测 → 无限生成新伤口（prevHp 同步修复）；感染必然线性增长 → 每道 cut 终将坏疽且冻结愈合 → 改概率触发。验证：30 分钟默认局 0 崩溃、伤口 37 条动态平衡（非堆叠）、感染 4 次可治、死亡 0。测试重写 7 用例（自然愈合/出血凝血/治疗语义/感染赛跑/部位权重/封顶/旧档迁移）。全量 334 测试 + tsc 干净 |
| 地道（穿水/穿山地下交通） | 用户 2026-08-14「添加地道，可以挖掘，快速旅行，作为地下交通来使用（速度没有加成，限制大宗物品通过）」 | ✅ **落地**：**tunnel 建筑 def**（木 4、buildTime 4、1×1、passable、无科技）——可在 water/mountain/可建地形上挖掘（穿山隧道/水下通道），草地可建（地窖入口），树格不可（先伐木）。**快速旅行 = 路线便利非速度**：寻路代价仍按地形 moveCost（地道格默认 1，无加成），价值在直线穿山过水不绕地形。**限制大宗物品**：1×1 窄道，格上不可建任何其他建筑（canBuildFootprint 冲突，存储/篝火均不可占），无工作/存储功能，仅单人通行。实现：`BuildingDef.onTunnel` + `canBuildFootprint` onTunnel 特判 + `isPassable` 地道格特判（建筑覆盖不可通行地形——顺带补上桥的 replacesTile 未消费的水上通道缺口，桥未动）。autobuild 只处理玩家蓝图不主动挖地道；queueBuild 拒因反馈自动适用；hg 兼容（内核 def 不依赖被卸载系统）。测试 +4（建造限制/通行/寻路直穿/无速度加成），全量 338 测试 + tsc 干净 |
| 地道维度化 + 道路 + 地形 z 高差判定 | 用户 2026-08-14 三次澄清：「地道不出现在地形上」「但是可以修建道路」「地形有z值，地形高差过大会导致无法上去，每个单位有各自的通过能力」 | ✅ **落地**（重设计，旧"穿水/穿山通道"语义作废）：① **地道 = 另一维度**——入口只能挖在**地表可通行格**（人先走到洞口；树上/水里/山里开洞无意义），入口之间**地下直连**（寻路虚拟边：成本=直线距离，速度无加成；无视地表地形/高差/建筑——"和地图不产生关联"）；地表**不渲染**（"不出现在地形上"）；入口 1×1 不可叠建筑（大宗限制）。② **道路**——地表坡道：可建在可建地形、正常渲染（"出现在地形上"）、**高差判定豁免**（修路=陡坡垫平，任何单位可沿路上下）。③ **地形 z + 通过能力**——TILES 全表补 z（grass/dirt/sand/desert 0、stone/ore/tree 1、mountain 2、water 0、bridge 1）；`isPassable(x,y,fromZ?,climb?)` 高差判定 `|Δz| > climb → 不通`（缺省不判=旧调用面不变）；`PawnTuning.climb=1`（PawnState.climb 各自能力，mod 可 overrideTuning）、enemyDef.climb（猫 2 > 鼠人 1：能爬石头追猎）。测试：地道组重写 + 新增高差/道路用例（旧穿水用例按新语义改造），全量 339 测试 + tsc 干净 |
| 第三轮试玩（地道/道路/z 剧本） | 用户 2026-08-14「再多玩玩」 | ✅ **验证**：① **地道穿水剧本**——双入口直穿 8 格水域（路径 2 步 vs 无地道绕行 11 步）、小人实走闭环（走到入口→维度跳跃→出口）。② **默认局 30 分钟**——0 崩溃、人口 4、建筑 8（z 判定不影响生存闭环）。③ **hg 30 分钟**——0 崩溃、人口 4→6（游牧不受影响）。④ **Δ2 石丘场景**（stone z1→z2 注入）——climb1 上不去（"高差过大无法上去"）、猫 climb2 能上（"各自通过能力"）、修路后 climb1 可上（道路豁免）。⚠️ **设计缺口**：当前 192×192 地图无 Δ>1 的可通行地形（mountain Δ2 但不可走）——高差阻挡仅对未来地形/mod 生效；另：入口不渲染（设计内），玩家靠建筑面板定位入口 |
| 生成高低差地图 + 寻路性能修复 | 用户 2026-08-14「生成高低差地图」 | ✅ **落地**：① **石丘**——stone z1→z2（石丘高地），山地边缘带（elevation 0.5-0.62）全石丘+矿；丘陵带（0.28-0.5）detail>0.6 出 stone 缓坡（低地 z0 → 缓坡 z1 → 石丘 z2 层次）。实测：石丘 314 格、绝壁边（z2 邻 z≤0）225 处、多种子出生点可达 58-72%（不被围困）。② **出生点连通 z 感知**——ensureSpawnConnectivity BFS 走鼠人 climb1 + z 判定，石丘围困时破口（水/树/石丘→草地）。③ **寻路性能修复**（石丘地图 24000 step 从 >70s → 4.6s，13x）：a. **失败路径也缓存**（getPath + 航点段；失败每帧重算 A* 满跑 maxIter 是主因）；b. **失效时机**：onTileChange（伐木/采矿/地形事件）→ clearTrailCache（否则"伐木后目标变可达"被旧失败缓存误拒），buildSystem 建成时已有；c. **nearestPassable/moveAdjacent 目标 z 感知**（石丘顶目标落到可攀格，不发起满跑寻路）；d. trailCache 上限 2048→8192（失败缓存占用）。④ 验证：默认局 30 分钟 475ms/0 崩溃/人口 7；hg 240ms/0 崩溃；修路翻越石丘 ✓、地道穿丘 ✓。测试 +1（高差地图：多 seed 出生可达 + 绝壁存在）、高差断言改 Δ2，全量 340 测试 + tsc 干净 |
| 性能 Review 第二轮（2026-08-14 石丘修复后） | 用户「继续 review 性能」 | ✅ **结论：常态性能已达标**（4 人局 0.24ms/step、30 人开局 0.41ms/step、8 房间并行 0.078ms/step/房间，均远低于 60fps 16.7ms 预算；预热后各系统峰值 <1ms，此前 san 64ms/behavior 22ms 峰值系首帧预热，无真问题）。**做了一项优化**：medicine 系统 mood/san 写回收口——原实现每伤口每帧各调 adjustMood（readNeeds+setNeeds+bus.emit 往返）及出血/感染分支独立写回，伤口多时一帧 6 次往返（profiler 定位：medicine 占 42% 耗时）。收口后每 pawn 每帧至多 1 次 mood + 1 次 san 写回（delta 语义不变，mood_changed 事件频率从每伤口每帧降为每 pawn 每帧，消费端仅 history 日志）。**效果**：常态 24000 step 5.7s→4.1s（medicine 2395ms→1296ms，-46%）；全员重伤极端场景 1.9ms/step（11% 预算）。全量 340 测试 + tsc 干净 |
| 无限地图：双图层 chunk + 负坐标 + 视口渲染 | 用户「继续优化性能，需要支持无限地图」+ 指摘「你仔细阅读docs 肯定有无限地图要求的，你又没有按照要求做」（DESIGN §205-214/§355-385） | ✅ **落地（模拟/网络/存档/渲染全链路）**：① **chunk(64×64) 双图层**——生成层（seededRng(chunkX,chunkY) 确定性，懒生成不落盘）+ 覆盖层（与生成层的差异才存/传输，改回生成层默认即删覆盖记录）；chunkKey 整数对支持负坐标。② **noise 坐标化**——tileAt(x,y,s) 纯函数（稀疏化从顺序 rng 流改坐标 hash），懒生成必需。③ **建筑 key 重编码**——x + y*2^31（负坐标支持），keyToXY 负坐标安全（Math.floor(key/2^31) 有浮点误差 → 先 % 取 x 再整除）；旧档（y*width+x）自动迁移（legacyKeyDecode）。④ **序列化**——serializeChunks（覆盖层 diff）/serializeTerrainChunks（完整地形快照，server tileGrid 用此：客户端无生成层算法）；SaveData.tiles 联合类型（string[] 旧档 | ChunkData[] 新档）。⑤ **客户端视口渲染**（DESIGN §384）——地表/地形图标按 chunk 缓存 Graphics，相机移动增量挂载/卸载视口外 chunk（原全图 192×192 一次绘制，探索远处黑屏/对象爆炸）。⑥ **出生区连通 BFS 出界即停**——inBounds 恒 true 后 BFS 会顺着可达大陆无限外扩生成海量 chunk（构造 >30s、npm test 240s 超时）；出界=视为已连通外部，不展开。⑦ **A* 堆过期节点丢弃**——碎片地图（稀疏化 hash 后树墙分割）下 cost 更新的旧拷贝反复 pop 浪费迭代，60000 迭代仍空路径；pop 时跳过 closed/更优 cost 节点。⑧ **修掉全部旧 key 解码残留**——pathfinding/socialUnit/needs/san/raid/repair/autobuild/build(replacesTile)/farm/card/dummyLlm/llm/renderer/server/remote/hunter-gatherer/power 等 17 处（新 key 下 `k % width` 解出错误坐标，桥不修/篝火不迁/伤口不疗全因此）。测试 +7（懒生成确定性/负坐标/覆盖层差异/完整地形往返/旧档 tiles+key 迁移/BFS 出界构造受控）。全量 347 测试（15s，原 240s 超时）+ tsc 干净 |
| Review 修复：thermo/prison 坐标解码 + keyToXY 异号 + 寻路缓存 climb | 代码 review（2026-08-14） | ✅ **落地**：① **`key % w` 旧解码残留（新玩法包）**——thermo.ts / prison.ts 用旧公式解新 key 编码（x + y*2^31），y 坐标解码成上亿假值：温场热源全错位（篝火旁永不暖）、囚笼捕获距离/招降 spawn 位置全乱；改 `World.keyToXY`。② **`World.keyToXY` 异号负坐标 bug**——JS `%` 是截断余数，x 与 y 异号时余数偏移 2^31（实测 (5,-7)→(-2^31+5,-6)），负区建筑解码全错；修复：|余数| > MAX_TILE 即判定偏移并回拨（世界坐标 ±2^21 与偏移量 2^31 永不重叠，判定可靠），-0 归一。③ **trailCache key 缺 climb**——寻路结果依赖单位通过能力（高差），缓存 key 只含坐标；单位 climb 差异化（接口宣称"各自能力"）会互相污染缓存（失败路径更致命）；key 统一加 `#c{climb}`（含航点段缓存）。④ **prison 断粮逃逸逻辑**——只放走"从未喂过"的囚犯，喂过之后断粮被无限关押；改为自上次进食（或入狱）起断粮超 2 周期即逃。⑤ **modpack.test.ts 旧解码残留**——`totem % width` 断言因宽松通过失去意义，改 keyToXY。⑥ **minCtx 构造顺序**——world 先于 mods 构造导致玩法包建筑 def 进不了 World 快照（placeBuilding 恒 false，prison/thermo 测试暴露）；改为先 mods 后 world 并传 defs（与 Sim 同姿势）。⑦ 清理 power.ts 死字段 lastForgeTick。回归保护：**prison.test.ts 5 用例**（捕获/招降 spawn 位置=笼旁 1 格/喂过+断粮逃/未喂+断粮逃/有粮则喂）+ **thermo.test.ts 3 用例**（半径外冻/火旁 vs 野外惩罚对比——热源解码错误显形/暖炉半径内外）+ **keyToXY 全象限往返 15 组**（异号组合此前被 (-5,-7) 同号掩盖）；红/绿验证：thermo 解码回归旧公式 3 用例全红。全量 356 测试 + tsc 干净 |
| 篝火烹饪 + 夜晚温度保持 | 用户 2026-08-14「（mod只在初始化时添加，不需要热插拔）火堆要能cook，要有夜晚温度保持」 | ✅ **落地**：① **cooking 玩法包**（src/mods/packs/cooking.ts，默认挂载）——篝火 = 简易炉灶：overrideDef campfire 补 `recipe:'cook'` + tags 加 'cook'；cook 配方（kind 'batch'）：4 food + 1 wood → 5 food（interval 4s，多座篝火共享节奏均分，缺任一食材不动锅——不烧仅有的口粮）；**独立 CookSystem**（不依赖 craft 系统）：hg 局卸载 craft 后篝火仍能烤肉（游牧烤肉世界观刚需）。数值：净盈余温和（< 农田 2 格产出）+ 木材消耗出口。② **夜晚温度保持**（thermo 包）：热源覆盖区内夜晚有效温度不低于 `nightWarmFloor=15`（环境夜 8°C/雨天夜 4°C 火旁恒 15°C 舒适；野外无火照常冷），白天不干涉。③ **决策记录**（用户明确 mod 仅初始化装配、无热插拔）：sim.ts World 构造处注释——defs 快照进 World 是既定设计，Sim 构造后注册的建筑进不了 world.buildingsDefs 属预期，不做动态支持（此前 minCtx 已按同姿势修正顺序）。回归保护：cooking.test.ts 4 用例（def 接线/首轮就绪+间隔+缺料不欠账/缺食不动锅/无篝火无事）+ thermo 夜保用例（火旁夜 15°C 无惩罚 vs 野外冻）+ assembly 装配断言 22 系统 + sim.test hook 探针改 ore（wood 被 cook 消费抵消，实测 +1 变 0）。全量 362 测试 + tsc 干净 |
| 香料玩法（野外香料丛 + 篝火加料烤制） | 用户 2026-08-14「香料等等玩法」 | ✅ **落地**：① **spiceBush 地形**（TILES 表）——可通行灌木（不挡路，出生圈连通/破口逻辑零特判）、growable+harvest 数据驱动自动接入采集（cardSystem workChop 谓词 `growable && harvest` 命中；采完变 grass）；产物 spice 走全局库存（私有化只针对 food）。生成：noise.ts 新增独立 spice 种子轴 + 草地 0.5% 密度点缀（192×192 约 74~113 株；独立轴改密度不影响其它地形）。② **物品 spice**（堆叠 99）。③ **cook_spiced 加料配方**——4 food + 1 wood + 1 spice → 7 food（interval 4s，多座篝火共享节奏均分）；CookSystem 改按配方分组：每轮优先加料（meta.cookSpiced 引用配方，库存够才用），香料耗尽自动回落基础配方（4+1→5）不熄火；日志 🧂/🍳 区分。④ **overrideDef 改深合并**（registry：此前浅合并嵌套 meta 整体替换——thermo 补 campfire meta.heat 后 cooking 再补 meta.cookSpiced 会把 heat 冲掉，两个包互斥；深合并后嵌套字段按 key 共存，数组/标量仍整体替换）。验证：模拟 30 分钟香丛 113→80（pawn 自动采集）、塞香料 5 分钟加料 5 次/基础 11 次、spice 持续消耗、food 净增 74；单株采集独立测试 + 世界生成多种子抽查（74~113 株）。回归保护：cooking.test.ts +2（深合并共存契约/加料优先+耗尽回落+缺粮不加料）、gather 香料采集用例（全局 spice + 采完变 grass）。全量 366 测试 + tsc 干净 |
| 完全插件化（三件套收尾） | 用户 2026-08-14「架构是否完全变成一切皆插件」→ 审查残留后裁决推进 | ✅ **落地**（内核残留 + 硬编码清单 + 双格式 mod 全部收口）：① **gather 迁出内核**——采集系统从 `SYSTEM_DEFS` 迁为 **gathering 玩法包**（`src/mods/packs/gathering.ts`，锚 `before:'build'` 保持原位），内核收窄为 **10 个社会骨架系统**（needs/san/desire/behavior/socialUnit/social/build/raid/population/events），默认装配 22 系统总数不变（内核 10 + 玩法包 12）。② **默认包清单外置为数据**——`src/mods/packs/playstyle.ts`：`DEFAULT_PLAYSTYLE_PACKS`（有序包 id 数组 = 清单）+ `defaultPlaystylePack`（聚合包，requires = 清单）；`ModRegistry.default()` 不再逐包硬编码 mount，改为 registerPack 清单 + mount 聚合包（依赖图按清单序自动 apply）；换玩法 = 换一份 requires 不同的 playstyle 包；清单 id 对照表防拼错（抛错而非静默丢包）。③ **旧式函数 mod 统一为 ModPack**——`hunter-gatherer.ts`/`demo-berry.ts` 从 `export default (m) => void` 改为 `{ id, requires, apply }`（hg 显式声明 requires:['gathering']）；消费方迁移到注册表 API（hg/demo-berry 测试、scripts/play.ts、client `?mods=` URL 加载器——URL 路径现接受 ModPack default 导出并 mount，函数式导出 deprecated 给警告）。回归保护：assembly.test.ts 断言改内核 10 + 玩法包 12 + 纯内核 10 可运行；uninstall.test 全量遍历自动跟随；hg 测试经 mount 装配（依赖图解析路径）。全量 366 测试 + tsc 干净 |
| 内核纯引擎（Stage1：8 系统全迁出 + 顺序清单化） | 用户 2026-08-15「内核怎么这么多东西，还要继续拆，内核真的只是一个引擎」 | ✅ **落地（分阶段推进第 1 期）**：原内核 10 个"社会骨架"系统再迁出 **8 个**为玩法包——needs/san/desire 合为 needs 包（紧耦合同写 PawnState 供 behavior 消费，disableSystem 仍可按 id 单独卸载）、social/build/raid/population/events 各成独立包（events 包照旧接线 SCRIPTED_EVENTS + mods.events + llmEventProvider）。**内核只剩 2 个引擎系统：behavior（决策引擎）+ socialUnit（派系单位契约）**，纯引擎 Sim（卸全部玩法包）= 2 系统可装配可步进（卸载不破坏核心契约，生存闭环由玩法包提供）。**顺序机制重构**：锚点目标（build/raid/events）自己也成了包 → before 锚点无处可插 → 执行序改为 **BASE_SYSTEM_ORDER 全量数据清单**（defs/systems.ts，22 系统数组位置即序，内核 2 系统内联 ctor、其余 20 由玩法包按 id 回填；清单外第三方包仍可用 before 锚点插位兜底）。默认装配 22 系统**顺序逐位不变**（实测 needs→…→autobuild 与迁移前一致）；hg 玩法局同理。playstyle 清单扩为 19 包（原 13 + needs/social/build/raid/population/events 6 个新包——san/desire 系统并入 needs 包）。回归保护：assembly.test.ts 重写（内核 = ['behavior','socialUnit'] 断言、PACK_IDS 20 系统、执行序 = 清单序断言、纯引擎 30 步可运行）；uninstall.test 遍历自动跟随。全量 366 测试 + tsc 干净 |
| 内核纯引擎（Stage B+C+D：0 系统 + 能力让渡 + 命令路由 + 经济/引导包） | 用户 2026-08-15 分阶段推进第 2-4 期（B+C+D 全套直抵纯演算框架） | ✅ **落地**：**内核 = 0 系统纯演算框架**——behavior/socialUnit 两个"引擎系统"也迁出为玩法包（behavior.ts / social-unit.ts），Sim 类体内残留的经济账本与出生引导抽为 economy/bootstrap 玩法包；`BASE_SYSTEM_ORDER` 扩为 **24 系统、无内联 ctor**（KERNEL_SYSTEM_IDS = []）。**能力让渡**：Sim 新增 caps Map + `provide/getCap`（SimContext 同步加 provide/getPath/initialPawnCount/selected/flow），玩法包构造时自报能力（behavior/socialUnits/economy/bootstrap 四项），`sim.behavior`/`sim.socialUnits` 变 getter（无包回落 null/NOOP，卸载不破坏核心）。**命令路由**：`issueCommand` 路由器——move 引擎内建，build/mine/assign/oracle 由玩法包 `registerCommand` 提供（拒建反馈/采矿/神谕/指派逻辑随各包迁出）。**economy 包**：记账（alpha 预期 + 情绪 + 资源流）+ 派系优先级评估，exec 位在 behavior 前（当帧生效）。**bootstrap 包**：出生刷人 + 初始营地 + 建篝火归属回调 + respawn/ensureCamp 能力（表尾执行，副作用不早于系统订阅）。**执行序**：needs→san→desire→**economy**→behavior→socialUnit→social→gather→…→autobuild→**bootstrap**（24 序，原 22 序逐位保持）。playstyle 清单扩为 **23 包**。回归保护：assembly.test.ts 重写（内核 [] / 24 系统 / economy 位序 / bootstrap 表尾 / 纯引擎 0 系统 30 步可跑）；sim.test 替换行为系统用例改能力让渡契约（disableSystem + 自建新 id + provide）。play.ts 实测：出生 4 人 + 篝火 + 聚居正常（bootstrap 闭环）。全量 368 测试 + tsc 干净 |
| 制衣玩法包（服装制作：材质 + 染料 + 设计=科技抽卡） | 用户 2026-08-15「添加服装制作服饰风格玩法」+ 澄清「染料+设计（=科技抽卡）+材质」 | ✅ **落地**：**clothing 玩法包**（src/mods/packs/clothing.ts，默认挂载，系统 'clothing' 插入产出组末尾 cook 后、raid 前——BASE_SYSTEM_ORDER 24→25）。**材质两路**：皮路线（猫击杀掉 pelt×2 → 鞣革 leather×1 → 缝制皮衣 peltShirt/warmth+3，leather×2 → 皮大衣 leatherCoat/warmth+6 需 craft:coat）；麻路线（flaxBush 野外采 → 织麻布 linen×2 → 亚麻衫 linenShirt/warmth-1 散热，需 craft:linen）。**染料**：red/blue/yellow 三色野外浆果丛（各 0.25% 密度，比香料更稀）→ 染料；9 个染色配方程序生成（任意衣 + 染料 → 染色衣，需 craft:dye），染色衣穿戴心情 +2（悦目）。**设计 = 科技抽卡**：4 个制衣科技（craft:clothing 解锁裁缝台 loom / craft:linen / craft:coat / craft:dye）走碎片制抽卡池；裁缝台 meta.tailor 配方表数据驱动批量生产（独立 TailorSystem，不依赖 craft 系统，cooking 先例；配方科技门控为**包内表** TECH_OF——用户指摘「内核为什么需要扩展」后撤回 RecipeDef.tech 内核字段）。**穿戴**：命令协议扩展 'wear'（处理逻辑全在包）——库存扣 1、旧衣回库存（换装不蒸发）、mood +3（染色 +5）、状态存 PawnState.extra.worn 存档扩展点（零内核改动自动随档）；渲染 tint 染色（红/蓝/黄三色）、HUD 穿衣行、play.ts wear 命令、协议 worn 字段 + items 可穿标记全链路透传（远程染色）。**thermo 联动**：穿着的 ItemDef.meta.warmth 调有效温度（正=御寒/负=散热）。**纯插件纪律**（本轮用户指摘后最小化内核触碰）：只动三个协议面——ItemDef.meta（对齐 BuildingDef.meta 通用容器，warmth/wearable 放 meta 不放顶层字段）、Command 'wear'（协议成员）、noise 4 种子轴（spice 先例）；RecipeDef.tech/itemOf/PawnState.worn/save-load 全部撤回（包内表 / ctx.mods.items 已有 / extra 扩展点）。**踩坑修复**：① registerTech 曾因 TECHS 全局表跨实例污染抛「explore:loom 已存在」——谓词/探索卡是实例级装配，改为覆盖式注册（注释记录）；② 测试断言 issueCommand 返回值踩空（返回 void）→ 副作用断言；③ 采集测试发现决策读 ECS Position 组件而非 pawnPositions 展示 Map——只 set pawnPositions 时小人从营地决策跑去砍树（近处 flaxBush 反而不采）。回归保护：clothing.test.ts 10 用例（装配位序/配方表/科技门控建造/皮路闭环/麻路科技门控/染色消费/wear 穿换脱+防蒸发/拒绝/thermo 保暖联动/野外采集）。全量 379 测试 + tsc 干净 |
| 制衣纯插件收敛（点缀数据化 + 命令协议开放） | 用户 2026-08-15 二次指摘「内核为什么需要扩展，不是说好纯插件吗」 | ✅ **落地**：上一轮"最小内核触碰 3 面"再收 2 面——① **生成层点缀数据化**：noise.ts 删 spice/flax/red/blue/yellow 五个玩法种子轴与 5 个写死点缀分支（玩法规则曾进内核 = 纯插件违约，spice 先例一并迁出）；`TileDef.sparse: { density, on? }` 声明（spiceBush 密度 0.005 迁入 defs 表、clothing 四丛由包注册时声明），World 构造时 `buildSparsePatches` 按 defs 收集序从主 seed 派生独立轴种子（构造时固定 → chunk 懒生成查表仍确定性；改任一密度不影响其它点缀坐标 hash）；BiomeSeeds 砍回 4 引擎轴（elev/moist/detail/sparse）。② **命令协议开放**：Command.type 删 'wear' 枚举成员、删 itemId 顶层字段 → `(string & {})` 开放字符串（IDE 仍提示已知命令）+ `args?: Record<string, unknown>` 通用参数位（内核不解释，玩法包自行读取——wear 命令的 itemId 先例）；wear 命令 100% 在包。③ **ItemDef.meta 保留**：对齐 BuildingDef.meta 的通用容器机制（thermo 跨包数据通道；tuning 类型封闭走不通、包间 import 违反"系统只依赖接口"）。验证：全量 379 测试 + tsc 干净（懒生成确定性/同 seed 同输出不受影响） |
| 包依赖显式化 + 自动组 DAG + 第一个插件 = 管理器 | 用户 2026-08-15「每个包需要前置包，显式写出」「放进框架的时候能够自动组成有向无环图」「第一个插件变成管理器」 | ✅ **落地**：① **每个包显式声明 requires**——24 个玩法包全部补 requires 字段（无依赖 = 空数组）+ 头部"依赖（2026-08-15 显式化）"注释（硬依赖 vs 可选联动区分：bootstrap→['socialUnit']、autobuild→['build'] 为硬依赖；thermo↔clothing meta.warmth、needs/social→socialUnit 记忆为弱依赖/可选联动，NOOP 回落安全不写 requires）。② **框架自动组 DAG**——pack.ts 新增 `topoSort`（Kahn 拓扑排序：闭包收集包+全部可达 requires → 入度 0 出队 apply，前置必在依赖者前；缺前置/环检出；同层相对序 = 输入序稳定）；registry.mount 由 DFS 挂载栈改为 topoSort——**清单顺序不再承担图约束**（乱序清单挂载序自动正确，测试证明）。③ **第一个插件 = 管理器**——PLAYSTYLE_PACKS 对照表迁入 playstyle.ts（数据归数据层），新增 `playstyleManager` 包：清单校验 + 组 DAG 拉齐全部玩法包都在其 apply 里；ModRegistry.default() 只剩数据种子 + mount(playstyleManager)，**框架不再 import 任何玩法包**（registry↔玩法解耦）。换玩法 = 换管理器/聚合包，框架零代码。回归保护：pack-deps +3（每包显式 requires / 乱序清单自动拓扑 / 确定性）。全量 382 测试 + tsc 干净 |
| 架构一致性重构（behavior 归内核 + 类别序推导 + 契约校验 + 色值解耦） | 用户 2026-08-15「现在架构如何…fix them」「逻辑要一致的。插件/mod 不要有不一致行为」 | ✅ **落地（四项修复，全量 386 测试 + tsc 干净）**：① **behavior 决策引擎归内核**——用户裁决"决策引擎本质是引擎服务，0 系统内核有叙事成分"：SYSTEM_DEFS 表内联 ctor（内核 = 1 系统），assign/oracle 命令从包迁回引擎协议面（Sim 构造注册，卸载 behavior 后命令仍在但无人消费=无害），behavior.ts 包删除、playstyle 24→23 包。**一致性原则**：内核系统与插件系统走完全相同的装配规则（类别推导/卸载过滤/锚点兜底），区别只有 ctor 来源；能力让渡（provide/getCap）与卸载语义对内核系统同样生效。② **执行序人工数组消除**——BASE_SYSTEM_ORDER 全量 25 序数组 → **类别语义序 CATEGORY_ORDER（7 类：needs→ai→society→production→raid→world→boot）× 组内注册序推导**（sim.registerSystems）；bootstrap category 改 'boot'（类别序恒表尾，无 hardcode）；playstyle 清单序调整（techPool/autobuild 移到 population/events 后）后推导执行序与旧表**逐位一致零漂移**（assembly.test 显式期望序快照断言）。**顺带修复 8 处脏数据**：各玩法包 registerSystemDef 的 category 历史遗留（thermo 曾写 needs、prison 曾写 raid、cook 曾写 needs、bootstrap 曾写 world 等 8 处）——旧架构 category 不影响顺序没人核对，新推导暴露后全部对齐 SYSTEM_DEFS 表；兜底循环补 isSystemEnabled（防禁用系统被追加回序）；推导循环只推表内 id（表外第三方包保留 before 锚点/表尾旧语义，防锚点被抢）。③ **跨包契约校验**——新增 src/sim/mods/contracts.ts：key 常量（K_WARMTH/K_WEARABLE/K_WORN，写方/读方/客户端/服务端一律引用，拼错=编译期错误）+ 登记表（写方/读方/类型/存在性谓词）+ validateContracts；playstyleManager apply 末尾严格校验（违例即抛错防回归，第三方管理器可降级）；单包自洽键（meta.dye/tailor/heat/cookSpiced）不入表；卸载写方=契约空真（不误伤卸载纪律）。④ **客户端反向依赖消除**——CLOTH_COLORS 从 clothing 包删除：色值=表现层数据，唯一权威在 renderer 本地 DYE_COLORS 表（服务端不再持有色值，杜绝两端不一致）；clothing 只持染料 id + 中文色名（物品名「红的粗糙皮衣」修复旧「#c8605a的粗糙皮衣」观感问题）。回归保护：contracts.test.ts 4 用例（默认满足/写错违例/卸载空真/常量对齐）；assembly.test 重写（内核=['behavior']/显式期望序/纯引擎=1 系统可跑）。 |
| 契约表扩展（命令 + 协议字段入表） | 用户 2026-08-15「继续fix」（上轮剩余项：命令参数/协议字段靠类型+测试兜底） | ✅ **落地**：contracts.ts 新增 **COMMAND_CONTRACTS**（wear 命令：args=['itemId']，发令方 hud/play.ts → 处理器 clothing；check=有衣物族则处理器必须已注册——捕获"有衣物可穿却无 wear 处理器"的静默失效，处理器随包 apply 注册、不随系统卸载消失=卸载不破坏玩法面）+ **PROTOCOL_CONTRACTS**（protocol.pawns.worn / protocol.items.w：server → client 字段语义唯一权威文档，编译期已有 shared/protocol.ts 类型保护）。validateContracts 统一检查三类契约（meta/extra + 命令 + 协议）。回归保护：contracts.test.ts 4→8 用例（命令默认满足/处理器缺失违例/卸载 clothing 玩法面仍在/协议登记表防误删）。全量 390 测试 + tsc 干净 |
| 全库属性位置审计与编排 | 用户 2026-08-15「重新审阅文档，重新审阅所有插件，合理编排各个属性的位置」 | ✅ **落地（23 个玩法包全量审阅 + 契约纪律补漏，全量 390 测试 + tsc 干净）**：① **契约读侧裸串修复×2（高）**——clothing wear 命令读 `meta['wearable']` 裸串（写侧用 K_WEARABLE 常量，拼错只改写侧=契约防线静默失效）+ server welcome 填充 `meta['wearable']` 裸串（跨层键服务端读侧，K_WORN 引了却漏 K_WEARABLE）→ 均改引 K_WEARABLE 常量。② **K_DYE 常量（中）**——item.meta.dye 本为单包自洽键不入表，但 CONTRACTS check 谓词依赖它做"衣物族"检测（裸串 'dye' 时键改名 = 校验防线静默失效）→ contracts.ts 定 K_DYE 常量，check 与 clothing 写/读侧（registerItem 染色版 meta + wear 命令 dyed 判定）共用。③ **协议值语义登记（中）**——protocol.pawns.worn 值 = 染色款 id `{dye}_{base}` 前缀（renderer split('_')[0] 解析染料 tint、server 原样透传），改 id 格式会静默破坏染色渲染 → 值格式约定写入 PROTOCOL_CONTRACTS。④ **trade 死字段删除（中）**——tradePost 声明 meta.trade.rate:1 但 TradeSystem 从不读（实际来访节奏 = CFG.visitMin/visitMax 随机间隔）→ 删字段 + 注释（属性声明与实现脱节）。⑤ **forge 配方重命名（低）**——配方 id 'forge' 与建筑 id 'forge' 跨命名空间重名（日志/科技易指错）→ 改 'forge_tools'（配方 id 不进存档零迁移）。⑥ **语义登记注释×3（低）**——medicine treat 卡 satisfies wrath 与内核 heal 卡双疗伤路径并存（treat 管伤口/heal 管血量）语义登记；wildmouse hostiles 手工快照与 raidSystem 生成路径重复登记（EnemyDef 增字段不自动透传）；gathering mine 命令与内核 caveMine 卡同写 st.mineTarget 覆盖优先级约定（玩家指挥优先）。⑦ **文档裁决（中）**——DATA_DRIVEN.md §13「数值位置：两种模式」：tuning 段（内核遗留系统可 overrideTuning）vs 包内 CFG（新玩法包私有平衡表，clothing/medicine/power/prison/thermo/trade 六包，注释头「玩法包自治」）——两种都是合理位置，以包新旧为界（历史继承非错误），明确"新玩法包数值一律包内 CFG 不塞内核 tuning"；属性位置总纲（静态数值/注册数据/跨包键/单包键/表现层/运行时状态/存档扩展七类各归其位）。 |
| 审计收尾（cooking tags 重列消除 + 文档演进注记） | 用户 2026-08-15「继续」（上轮审计低项 + 文档审阅剩余） | ✅ **落地**：① **cooking tags 全量重列消除（重复维护点）**——cook 判定原为 `def.recipe==='cook' && tags 含 'cook'`（tags 是 cooking 自己加的无意义约束）→ 改 recipe-only 判定；cooking overrideDef 不再重列 tags（内核 campfire 原 tags 自动保留，未来内核加 tag 不丢失）；cooking.test 断言改防回归（tags 不含 'cook' 但保留 'anchor'）。② **DESIGN.md §20 演进注记（文档审阅剩余）**——§18"已知差距"（玩法内置内核）与 §19"内核收窄 11 系统"为当时快照，已过时；按"只加不减"铁律追加 §20：Stage1 → 0 系统纯演算 → behavior 归内核 1 系统的演进衔接 + 类别序推导/能力让渡/契约表终态描述。③ 复核：clothing cat 依赖注释（「cat 是内核 defs，可选联动」）、bootstrap 3×3 布局注释均已具备，无需改。全量 390 测试 + tsc 干净 |
| 审计第二轮（全 sim 数据面：tuning/存档/客户端读取面） | 用户 2026-08-15「继续」（属性位置审计扩展） | ✅ **结论为主 + 文档裁决（全量 390 测试 + tsc 干净）**：① **tuning 全表 379 键零死参数**——脚本扫描定义键 vs 全库消费引用（tsx 遍历 + 完整路径/父段兜底匹配），唯一疑似 trio（world.spawnCounts.tree/ore/stone）为 Record 动态键误报（消费方 Object.entries 遍历）；数据驱动纪律无死角。② **存档字段三层边界文档化（DATA_DRIVEN §14）**——顶层长期态（dna/skills/desires/inventory/oracleBuff/assignedJob/fireId/knownFires/techs 等白名单显式随档）/ extra 扩展点（worn/wounds/captive/charge 原样 JSON 还原）/ 瞬时工作态（path/targets/mining/冷却不随档=有意轻量存档，读档回闲置重新决策）。③ **PawnState.climb 差距登记**——注释声称"单位各自能力"，实现 = spawn 取 tuning 后从不修改（存档不还原无差别）；当前行为正确，未来个体差异需加 save 字段；enemyDef.climb 差异化不受影响。④ **客户端读取面干净**——hud/renderer/remote 全部引用 K_* 契约常量、零裸串 meta['…']；remote 重建 mods.items 只透传协议 w（wearable），warmth 不下发（sim 内部数值，渲染/穿衣无需）为设计。 |
| 审计第三轮（defs 表死字段扫描 + 清理） | 用户 2026-08-15「继续review？」（属性位置审计扩展） | ✅ **落地（全量 390 测试 + tsc 干净）**：脚本扫描 18 个 defs 接口 135 字段的消费引用 → 5 疑似，2 误报（InterestDef.weightMul/TraitDef.weightMuls——消费方在 weightRules.ts 的 ruleInterest/ruleTrait，扫描排除列表包含定义文件导致）。**真死字段清理×2**：① `TileDef.resourceYield`——注释自称"兼容旧引用"，旧引用早已不存在（采集全走 harvest 表）：删字段定义 + tree/spiceBush/ore 三处数据 + clothing 亚麻丛/染料丛写方。② `BuildingDef.workRadius`——语义已被 moveAdjacent/walkAndWork（走到旁工作）替代，无消费方：删字段定义 + campfire/farm/workbench/cave/church/monument 六处数据 + clothing 裁缝台写方。**保留+注释×1**：`ItemDef.stackable/maxStack` = 未实现的库存语义（当前库存 Record 统一计数无上限，只有写方无读方）——位置合理（ItemDef），API 意图字段，删了外部 mod 写方编译错，保留作未来"不可堆叠/库存上限"实现位。 |
| stackable/maxStack 彻底删除（第三轮审计再裁决） | 用户 2026-08-15「处理」（不留无消费字段） | ✅ **落地（全量 390 测试 + tsc 干净）**：上轮"保留 + 注释（API 意图字段）"被用户裁决否决——**不留"预留"字段（预留 = 猜测需求），无消费者的字段一律删**：ItemDef 接口删 stackable/maxStack（附注释：未来实现"不可堆叠/库存上限"时在 ItemDef 重新声明即可）；清理全部写方——ITEMS 表 5 物品（wood/ore/water/food/spice）、clothing 包（原料 7 项 + 衣物 3 款 + 染色 9 款 + maxStack 50/99 常量）、demo-berry.ts、remote.ts 协议重建（只留 id/name/meta）、sim.test.ts 2 处注册（herb/drink）、外部 mod 样例（demo-berry.mod.json + defs.json）。ItemDef 现在只有 id/name/meta 三个字段。 |
| 协议审阅：diff 建筑 key 魔数 bug 修复 | 用户 2026-08-15「继续review？」（审计第四轮：服务端/协议面） | ✅ **落地（真 bug，全量 391 测试 + tsc 干净）**：审阅 server/diff.ts 时发现**跨端身份 key 编码不一致**——diff 用魔数 `x + y*1000000` 重拼建筑 key（注释自称"与协议语义一致"），而协议 DeltaMsg.buildings.key 消费方 = 客户端 `World.keyToXY`（**2^31 编码**）：y≠0 建筑（如 (3,3)）delta 到达客户端解码成 (3000003→1000000+3, 0) **坐标错乱**，且与 snapshot 条目（buildKey = x + y*2^31）**身份不一致**（同建筑两个 Map 条目），5s 全量对账才纠正（y=0 建筑碰巧正常——魔数 1000000 与 2^31 同模小值）。**修复（统一编码）**：① SnapshotMsg.buildings 增加 `key` 字段（World 编码 x + y*2^31，注释说明）；② server buildSnapshot 填 key；③ diff.ts 两处（buildDelta/prevB/curB + fullDelta）改用 b.key 直传，删魔数；④ 客户端 applySnapshot 直接用协议 key（不再 buildKey 重拼；getBuilding 查询仍 buildKey=同编码一致）。**回归保护**：diff.test 新增"建筑 key 往返"用例（4 组坐标含负 y/异号 [3,3]/[10,-2]/[-5,4]/[-7,-1]——World.keyToXY 解码 = 原坐标，守护魔数不复活）；旧断言 3+3*1000000 → 3+3*2**31 同步。 |
| 协议审阅（续）：cmdValidate 白名单僵化 + 负坐标拒绝 + delta worn 链路断裂（3 处真 bug） | 用户 2026-08-15「继续」（审计第四轮：服务端校验/客户端合并面） | ✅ **落地（全量 394 测试 + tsc 干净）**：**① 命令白名单硬编码拒绝玩法包命令**——cmdValidate 曾硬编码 CMD_TYPES（build/oracle/assign/move/mine），命令协议开放（registerCommand）后 clothing 的 **wear 命令被服务端拒绝 = 远程无法穿衣**。修复：白名单 = `sim.mods.commandHandlers ∪ {'move'}`（move 是引擎内建分支不进注册表，首版漏 → 测试暴露）；坐标强制只对 move/mine，其余命令校验 pawnId 存在性（hud 发 wear 带 x:0/y:0 占位）。**② 负坐标被命令校验拒绝**——`isCoord` 曾要求 `v >= 0 && v < width`，无限地图负区（负坐标合法行走/建造）无法远程指挥。修复：范围 = ±MAX_TILE（越界拒 3e9）。**③ delta worn 链路两端断裂**——server diff 已发 worn 变化，但 a) 客户端 applyDelta 合并表漏 worn（穿衣 tint 等 5s 对账才刷新）；b) server 脱衣时 worn=undefined → **JSON.stringify 丢弃字段** → 客户端无法区分"没发"与"脱下"，脱衣 delta 无效。修复：server 归一 `worn ?? ''`（buildSnapshot + diff.ts 两处），协议注释 + PROTOCOL_CONTRACTS 值语义登记 `''` = 无穿着；客户端合并补 `pd.worn !== undefined` + `|| undefined` 归一（wornOf 返回值统一）。**回归保护**：cmdValidate.test 新增"wear 放行/未注册拒绝/负坐标放行"用例（含首版 move 漏白名单被测试暴露的记录）；protocol.test 新增 delta worn 穿衣/脱衣即时更新用例。 |
| RW-1 M1 工作优先级（Work Tab） | 用户 2026-08-15 RW-1 sprint「像 RimWorld Work Tab 一样设定每项工作优先级」 | ✅ **落地**：新玩法包 `work-priority`（默认挂载，`src/mods/packs/work-priority.ts`）。**数据模型**：`PawnState.extra.workPriorities = Record<jobId, 0|1|2|3|4>`（缺键 = 未设置 = 自主；0 = 禁止）——随档扩展点零内核改动。**抽卡调制**：`ruleWorkPriority` 权重规则（`before:'job'`，紧贴职业卡前）——未设置的工作不调制；0 = 该卡权重清零；1~4 = 乘 `{1:6, 2:3, 3:1.5, 4:0.7}`（多 job 同卡取最优 = 最低数字档）。紧急需求不抑制（需求分支在 decide 前，测试锁定）。**旧档兼容**：`migrateFromAssignedJob` 幂等迁移（assignedJob 主职 → 主职 1 其余 0）；assign 命令强制 shortcut（`applyAssignedJobShortcut`：assign = 主职 1 其余 0，取消 = 清空回自动）。**协议/UI**：snapshot/delta 携带 workPriorities（对象整体 diff）；HUD 工作面板（⚙ 按钮）按 job 行 × pawn 列网格，点选循环 空→1→2→3→4→0→空 发 set-work-priority 命令；选中面板工作摘要（优先级/禁止/全自动）。cmdValidate 校验 job ∈ JOB_CARD、priority ∈ 0..4。回归保护：work-priority.test.ts 11 用例（抽卡调制/禁止/0 抑制/未设置自主/紧急不抑制/迁移幂等/旧档往返/set 命令写清/assign shortcut/存档往返/远程 delta）。全量 405 测试 + tsc 干净 |
| RW-1 M2 征召战斗（drafting） | 用户 2026-08-15 RW-1 sprint「征召小人手动指挥作战」 | ✅ **落地**：新玩法包 `drafting`（默认挂载，系统 'drafting' category 'raid' 组内 raid 后——SYSTEM_DEFS 表 25→26 系统）。**数据模型**：`PawnState.extra.drafted = boolean`（征召中）+ `PawnState.extra.attackTarget = { hostileIndex, x, y }`（指定攻击目标 + 位置快照）——随档。**征召语义**：behavior 决策循环加**征召门**（K_DRAFTED 契约键，位置在理智分支**前**= 完全听指挥、连崩溃乱跑也不执行；被动衰减不豁免）——征召小人不抽卡/不工作/不休闲/不吃不睡不治疗、保持站位；玩家 move 命令 path 照走（测试锁定）、解除征召立即恢复自主。**追击驱动**：DraftSystem 每 tick 对征召小人发 engine moveTo（重寻路节流 0.4s 防 A* 风暴、尊重手动命令 commandCooldown 3s 不抢指挥）；指定目标优先（hostileIndex 击杀 splice 后按快照位置 targetLostRadius 找回、找不到清指定回落自动），无指定 = 半径 14 格内最近敌人自动接敌。**战斗零复制**：贴近后 raidSystem 结算原样生效（征召小人也在接敌循环）；raidSystem 只改"谁接敌"——`attackDesignatorOf` 优先指定者（在其 meleeRange 内），未征召小人自动接敌不变。**命令**：`draft {drafted}`（征召/解除，batch 走 selected，征召清工作态+路径、解除清攻击指定）+ `attack {hostileIndex}`（必须已征召，热身反馈）。**UI**：选中面板 ⚔⚔征召/☮解除征召按钮（批量）；被征召小人画**反缩放恒定大小圈环**（radius/线宽 × 1/zoom）；右键敌人 = 选中征召队 attack（未征召选中 = 事件 feed 反馈提示）。**协议**：snapshot/delta 携带 drafted（标量 diff，缺省 = 未征召）；cmdValidate 校验 draft.drafted 布尔 + attack 的 pawnId 存在 + hostileIndex ∈ hostiles 范围。回归保护：drafting.test.ts 9 用例（装配含命令/征召不自主+被动衰减不豁免/解除恢复/征召中 move 有效/攻击移动+伤害/指定优先于最近/存档往返/远程 delta/校验拒绝）；assembly.test 26 系统期望序更新。全量 415 测试 + tsc 干净 |
| RW-1 M1 工作优先级撤回 → 神谕卡式工作引导（oracle-guidance 包） | 用户 2026-08-15 裁决（**发起人自认错误**）：「我不该把它作为 M1 写进 prompt」——Work Tab 逐小人数字优先级 = 直接管理意图进入行为选择链，违背核心设计一切皆抽卡 / 神谕不碰选择链 | ✅ **落地（全量 414 测试 + tsc 干净）**：**M1 全部撤回**——删 work-priority 包 + 11 个测试 + `K_WORK_PRIORITIES` 契约键 + `PawnState.extra.workPriorities`（sim.ts/cardSystem/weightRules/protocol/server/diff/cmdValidate/remote/pawn 全部还原为 oracleGoal 目标层语义；assign 命令原样保留；`migrateFromAssignedJob` 撤回，旧档 extra 残留键无人消费 = 无害，契约键已删不误伤加载）。**修订 M1 = 新玩法包 oracle-guidance（默认挂载）**：玩家工作影响只走**神谕通道**——① `strategy {cardId}` 命令（面板/协议通用；冷却闸 `WeakMap<SimContext,number>` 45s 瞬态、权威在包内）；② `setOracleGoal` 目标层（对应工作卡抽卡权重 ×`oracleGoalMul` 3，不碰选择链——规则级测试锁定 ×3 与非目标不动）；③ 蓝图副作用走 build 命令入队（垦田令→农田/拓荒令→营地，队列去重"只入队一次"）；④ 可选 printCard 插"目标卡/习惯卡"（id 前缀 `strategy:`，选中才插，插后仍走抽 3 选 1——非保证）。**新增策略卡 2 张**：伐木令/采矿令（条件读 tuning 路径，2026-08-13「伐木令退位为可选神谕目标」定案落库；随机神谕会采样它们——引导非平衡）。**内核唯一扩展** = `SimContext.printCard`（Sim 已实现的 LLM 印卡通道；插件不可达小人槽位故入接口）。**UI**：HUD 删 Work Tab（CSS/按钮/面板/点击委托/选中摘要全撤）+ 新「⛪ 神谕/策略」面板（生效目标+剩余、冷却估算、卡列表可用态 = `evalStrategyCondition`（StrategyCtx = SimContext 子集，SimView 组装 slim ctx）；远程观看模式无 strategyCards 数据源 → 按钮隐藏，降旨为单机玩法）。**测试**：oracle-guidance.test.ts 10 用例（装配/设置目标/权重×3 但抽卡非必然（目标卡缺池永不出现）/违抗事件/蓝图只入队一次/冷却+持续/存档 JSON 无 workPriorities+drafting 等共存键干净/远程 snapshot+delta 无该字段/插卡选择性）；dummyLlm.test 缺粮垦田场景补木矿充足隔离（新卡条件匹配才会命中旧期望）。零 `workPriorities` / `work-priority` / `K_WORK_PRIORITIES` 代码引用残留 |
| RW-1 M2 征召战斗（drafting） | 用户 2026-08-15 RW-1 sprint「征召小人手动指挥作战」 | ✅ 保持不变（上一轮已落地）：见上一条 drafting 完整记录；本轮为其清掉 `workPriorities` 快照字段（buildSnapshotLike 类型对齐修订后协议），9 用例全绿 |
| RW-1 收尾验证（2026-08-15 三轮验证 + WSS 冒烟） | 完成度检查清单（含 30 分钟长跑 + 协议冒烟） | ✅ **收尾验证（全量 414 测试 + tsc 干净）**：① **30 分钟长跑 ×2**（实际 52 分钟、超出要求）——默认玩法 262 sim-分 / 18 人 / 9 科技解锁零崩溃；hunter-gatherer 232 sim-分 / 74 人 / 资源持续增长零崩溃（观察：默认档后期木 0、食物 473 稳定——符合 2026-08-13 定案"伐木令退位不做经济平衡"留待后续平衡议题，非本轮缺陷）。② **WSS 冒烟**：服务端接收并执行 draft + strategy 命令（`[server] cmd` 均达、零 workPriorities 于 snapshot/delta/命令面）；快照底与全部 delta JSON 均不含 workPriorities；坏 pawnId 命令被权威校验拒发（行为正确）。③ **零残留**：`workPriorities` / `work-priority` / `K_WORK_PRIORITIES` 全库仅存在于文档与测试反断言（无代码引用）。收尾文档：RW_SPRINT2.md（修订计划）→ DESIGN §22（双通道裁决）→ DATA_DRIVEN §15.3（包数据面）→ PLAYING（神谕/策略面板玩法）。 |
| RW-1 收尾补验（卸载安全 + 旧档兼容两缺口）+ WSS drafted 同步终验 | 完成度核验发现两处"计划勾选但未实测" + drafted 冒烟初次未得确定结论 | ✅ **补验（全量 416 测试 + tsc 干净）**：① **卸载安全实测**——oracle-guidance.test ⑨：清单去掉 oracle-guidance 的自定义管理器装配（default() 同款种子 + validateContracts），Sim 正常装配/步进 60 帧，无 strategy 命令处理器、无伐木令/采矿令残留（内置垦田令照常），发 strategy 命令 = 引擎无处理器 no-op 不抛。② **旧档兼容实测**——⑩：向存档 pawn.extra 注入被撤回的 workPriorities 残留键（模拟 M1 时代旧档）→ load 不抛（extra 透传、无迁移钩子），残留键原样保留无害、drafted 共存键一并保留、读档后决策循环照跑。③ **WSS drafted 同步终验**——全新 server 冒烟：连上即发 draft，首份 delta 携带 drafted:true + 全程 wire 零 workPriorities（初次冒烟因 pawn 快速死亡 + 已征召态拿不到增量，重做后明确通过）。总用例 414→416 |
| 趣味回放整理（RW-1 演示剧本 seed 124） | 用户「玩玩看,整理一些有趣的 replay」 | ✅ 新增 `scripts/replay-rw1-demo.ts`(固定 seed 124 确定性三幕:神谕引导/征召战斗/拓荒迁徙,WeakSet 去重防 sim.events 50 条环丢事件——曾踩坑)+ `docs/REPLAYS.md` 完整时间线与观察笔记:① 玩家与神谕同刻降「伐木令」,目标期伐木×12 主导但 2:21/2:25 两度违抗(神谕只引导不强制,实锤 DESIGN §22);② 6 猫浪潮征召 3 人,阵亡 3(2 名征召兵),幸存者带伤、战后内讧(征召非无敌);③ 拓荒令 1 秒落地新篝火@(84,86),营地 3 处分家。违抗强迫性实验:降旨+钳低心情扫 100 种子 0 次违抗(低心情走崩溃分支与违抗互斥)——违抗为自然概率事件,单元测试 oracle-guidance.test ③ 已覆盖 |
| DLC 框架压力测试（2026-08-16） | 用户点饼「以后要加大航海/一战/无线电/二战/飞天魔法 DLC」,先验证现在加会不会搞坏框架 | ✅ 新增 `src/mods/__tests__/dlc-framework-stress.test.ts`（6 用例,5 个内联占位 DLC 包走真实注册面:系统/命令/卡/科技/建筑/物品/敌人/配方/策略卡）。**结论:框架现成,无需动内核**——① 五 DLC 乱序挂载 = 拓扑自动拉齐（二战 requires 一战、飞天魔法 requires 无线电,闭包 + 先于依赖者）;② 单包挂载 26→27 系统、组合 31 系统步进/命令正常;③ 卸载（不挂）= 默认装配零回归;④ 冲突显式化:registerBuilding('campfire')/registerCommand('draft')/表外系统同 id 二次注册全抛"已存在",DLC 不能悄悄覆盖核心;⑤ 契约不误伤:未登记 DLC 命令 validateContracts 零违规。**踩坑/语义备忘**:requires 挂在全局包目录——直接 mount 缺前置包会显式报错（DLC 商店/清单先 registerPack 预登记即可,同 playstyleManager）;内核表内系统（behavior）同 id 注册被装配**静默忽略**（内核赢,防覆盖既定语义）;move 是引擎内建不走 commandHandlers,命令撞车面只在 mod 命令层;占位系统 ctor 必须给 id+update（裸对象能注册但 step 会炸）;表外系统 before:'raid' 锚点插位正常 |
| DLC 框架压力测试·追加帝国/教国（2026-08-16） | 用户继续点饼「还有帝国教国什么的」 | ✅ `dlc-framework-stress.test.ts` 扩为 7 占位包/7 用例:帝国 requires 大航海（海上殖民线）、教国 requires 无线电（传教线）;世俗线六 DLC 乱序合挂 32 系统步进不崩、教权线单挂 28 系统正常。**新演示的边界:互斥世界观（帝权 vs 教权）框架无内建机制——由包 apply 主动 throw 表达选线冲突,失败不污染挂载器（教国挂不入、帝国保留、后续挂载正常）**;目录预登记 + 拓扑闭包对 2 层依赖（sail→empire、radio→theocracy）同样自动拉齐 |
| DLC 框架压力测试·按指示改独立包（2026-08-16） | 用户「别瞎想。就是单独的 dlc」+「继续」 | ✅ dlc-framework-stress.test.ts 依指示修正:7 个占位 DLC（大航海/一战/无线电/二战/飞天魔法/帝国/教国）全部 requires 空数组 = 相互独立,删除自造的"帝国依赖大航海/教国依赖无线电"与"互斥世界观"戏码。验证面更新:七独立包乱序合挂 33 系统步进不崩（requires 全空 = 清单顺序不承担图约束）+ 新增 ⑥ **DLC 系统与默认系统同一卸载语义**:mount 后 disableSystem('dlc:ww1') → 装配过滤只收 2 个 DLC 系统、步进正常,命令处理器仍随包在场（命令契约语义:随包不随系统）。修一处测试自身粘连:④ 的 radio-call 曾靠"sky requires radio"连带注册,独立化后必须显式挂 dlcRadio |
| DLC 框架压力测试·加 2077（2026-08-16） | 用户「加入2077DLC,有史安威斯坦,义体什么的」 | ✅ dlc-framework-stress.test.ts 扩为 8 独立占位包/7 用例:新增 `dlc-2077`（赛博朋克 2077）——斯安威斯坦义体物品（ItemDef.meta 通用容器，本包自读）、义体医生诊所建筑（dlc:ripperdoc）、义体技术科技（dlc:chrome）、义体强化令策略卡、巡逻无人机敌人、install-cyber 命令;② 八包乱序合挂 34 系统步进不崩;新用例 ⑦:2077 单挂注册面齐活（items/buildings/strategyCards/command 断言）+ **before:'craft' 锚点把义体系统插进 production 组内**（表外系统锚点插位此前只在 raid 组验证过,本轮补 production 组）:dlc:cyber 在 craft 前、needs 后、bootstrap 前 |
| DLC 真实部署（mods/ 目录自动扫描，2026-08-16） | 用户「这些dlc可以随便写一点什么。然后你再看一下怎么部署,应该有自动的」 | ✅ 部署机制确认并落地:**自动的**——服务端 `loadModsFromDir(MODS_DIR='mods', registry)`（server/index.ts 启动时,`MODS_DIR` 可用环境变量覆盖）扫描 `mods/*.mod.json` → parseModPackage + buildModMount 自动挂载。8 个 DLC（大航海/一战/无线电/二战/飞天魔法/帝国/教国/2077）写成真实 `mods/dlc-*.mod.json`（manifest + defs 纯数据声明:物品/建筑/科技/策略卡/敌人/事件,内容随手占位;2077 含斯安威斯坦义体 + 义体医生诊所）；新增 `src/server/__tests__/dlc-deploy.test.ts` 集成验证:**放文件即部署**——全量加载 ok、errors 空、8 id 齐、内容注册（sandevistan/ripperdoc/palace/crusade…）、Sim 步进不崩。**部署面分界**:defs 白名单只有内容表（纯声明不改系统装配,8 DLC 零系统）;系统/命令入口在 scripts 函数式扩展（demo-berry 的 berrySpoil 系统先例 → 全量 mods/ 装配 27 = 26+1） |
| DLC 部署继续:2077 义体玩法 scripts + 端到端启服验证（2026-08-16） | 用户「继续做」 | ✅ mods/dlc-2077.mod.json 升级:scripts 义体玩法——`dlc:install-cyber` 命令(给选中/指定小人植入斯安威斯坦,状态写 pawn.extra.cyber 存档扩展点随档,单包自洽键不入契约表)+ `dlc:cyber` 系统(before:craft 锚点插 production 组,60s 触发神经加速叙事);dlc-deploy.test 同步:全量 mods/ 装配 28(26+berrySpoil+dlc:cyber)、其余 7 DLC 纯 defs 零系统(声明式 vs scripts 分界活示例)、install-cyber 命令验证(事件 + extra.cyber 随档)。**端到端启服验证**:`npx tsx src/server/index.ts` 启动日志 `mods=[demo-berry, dlc-2077, …, totem]` 10 包自动加载、服务正常监听。PLAYING.md 追加「DLC 内容包」段落(玩家可见:放文件即部署,MODS_DIR 可换,本地单机不带) |
| DLC 设计参考文档（2026-08-16） | 用户「可以参考一些其他游戏,你可以找一下」 | ✅ 新增 `docs/DLC_REFERENCE.md`:为 8 个 DLC 从成熟游戏找可落地的具体机制并映射到框架注册面——总纲 = RimWorld 4 个 DLC（Royalty/Ideology/Biotech/Anomaly）的"独立 DLC = 一条玩法轴"模式;每个 DLC 各有参考:RimWorld 灵能（飞天魔法:冥想充能→消耗施法）、钢铁雄心后勤（一战:弹药断供伤害减半/堑壕驻守）、Frostpunk 信号塔（无线电:持续广播+呼叫援助）、CoH 炮击（二战:区域延迟轰炸）、文明奇观/CK3 教条（帝国/教国:奇观光环/戒律=卡权重调制/仪式事件）、Cyberpunk 2077 本体+Deus Ex+RimWorld Biotech（2077:斯安威斯坦主动技=5s buff+冷却）。每机制都落到具体 API（系统/命令/策略卡/事件/aura/markov/class 卡池）+ 最小实现顺序建议。诚信声明:web 检索工具本轮 API 认证不可用,参考基于既有游戏机制知识,条目可复核 |
| DLC 设计参考·按裁决改「尽量基础,交给涌现」（2026-08-16） | 用户「遵从本项目基本思路:尽量基础,剩下的交给涌现」 | ✅ docs/DLC_REFERENCE.md 重写:移除全部人工机制链(主动技计时/延迟轰炸撮合/离营远征/弹药断供规则/法力硬约束/仪式调度/民怨阈值),只留**种子库**——每 DLC = 物品/建筑/科技/敌人/事件/卡/策略卡/工作/命令(数据+基础行为),玩法=模拟涌现(卡池权重→行为倾向、事件随机→故事、生存压力+门控→决策、玩家基础命令干预);参考游戏只用来选种子。2077 义体降级为"事实+叙事"(install-cyber 命令=与 move/mine/assign 同类的基础指挥 + 60s 叙事;主动技/战斗 buff 移除);二战炮火令=纯策略卡引导、飞天魔法=冥想工作进卡池(小人自己想不想冥想)等。已部署 mods/*.mod.json 本就是种子形态,无需改动 |
